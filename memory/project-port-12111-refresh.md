---
name: project-port-12111-refresh
description: "Линия MC 1.21.11 обновлена до актуала и ЗАКРЫТА 2026-07-08: core НОВАЯ ветка 1.21.11@3527d63 + редактор merge main->port/1.21.11@ae4936d; ревью V1-V3 PASS 0 blocker/major; jar в Desktop/IRLights/1.21.11/; worktree убраны; ин-гейм за юзером."
metadata: 
  node_type: memory
  type: project
  originSessionId: a4cbd228-8622-4d93-9fb1-677218138c68
---

Статус 2026-07-08 ФИНАЛ: ЗАКРЫТО. Коммиты по команде юзера («коммитим»): core ветка 1.21.11 = 3527d63 «Port to MC 1.21.11 (yarn 1.21.11+build.6, Iris 1.10.7; raw-GL shadow backend, RawOccluderBatch seam)» (от 1.21.4@1ce93fc; 15 файлов, 1037+/623-), редактор port/1.21.11 = ae4936d «Merge main into port/1.21.11» (родители 6aae374 + e147571). Push НЕ делался. Оба worktree _wt-* УДАЛЕНЫ (gradlew --stop -> rm -rf -> prune; long-path проблем не было), оба репо вернулись к одному дереву (core main@cf4ad94, редактор main@e147571).
- Сборки: core build+publishToMavenLocal SUCCESSFUL (intermediary подтверждён javap), редактор build SUCCESSFUL 4m34s (JiJ irl-core-1.1.jar 117478 B intermediary-1.21.11).
- Ревью: V1 / V2 / V3 — ВСЕ PASS, 0 blocker / 0 major (блок РЕВЬЮ ниже). Фикс-раунд не понадобился; единственный код-фикс после ревью = повисшая javadoc-ссылка ImmediateOccluderBatch -> RawOccluderBatch в core OccluderBatch.java (вошла в коммит 3527d63).
- Доставка: Desktop/IRLights/1.21.11/ = irl-core-1.1-mc1.21.11.jar + irl-redactor-1.21.11.jar (редактор JiJ-несёт core — в mods достаточно одного redactor-jar; jar собраны до коммитов, код идентичен закоммиченному — после сборки менялся только javadoc-комментарий).
- mavenLocal org/qualet/irl-core/1.1 теперь = 1.21.11-intermediary (перезаписал 1.21.4). Перед сборкой других линий re-publish соответствующей ветки core.
- ОТКРЫТО: ин-гейм runClient-проверка 1.21.11 за юзером (команда коммитить дана без явного отчёта о ран-тайме; чек-лист: редактор J, энтити-силуэты через новый шов, cutout-дырки, point 6 граней) + followUps ниже.

irl-core 1.21.11 — состав порта (агент C1, Opus, build зелёный):
- build.gradle: MC 1.21.11, yarn 1.21.11+build.6:v2, Iris 1.10.7+1.21.11-fabric; Loom 1.15.5 ПЕРЕВАРИВАЕТ 1.21.11 (fallback 1.14 не понадобился); Java 21/loader 0.16.14 без изменений; gradle.properties не тронут (1.1).
- Тривиальное: FramePipeline (getCameraPos; EntityRenderManager.configure(camera,entity) 2-арг), IrlSamplersBind (4-арг addDynamicSampler(TextureType.TEXTURE_2D, glId, ()->null, name)), BlockShadowCollector (BlockRenderLayers.getBlockLayer -> enum BlockRenderLayer; cutout+cutoutMipped СХЛОПНУТЫ в один CUTOUT), 6 GL-фильтров (только импорт GlStateManager -> com.mojang.blaze3d.opengl; MSM4/EVSM/pyramid не тронуты).
- ShadowRenderer: слой рисования переведён на raw-GL по образцу редакторской port/1.21.11 (2 GLSL-программы #version 150: pos-only depth + textured cutout с блочным атласом и discard alpha<0.5; свои VAO/VBO; VBO-кэш на сырых GL-id Long2IntOpenHashMap вместо удалённого net.minecraft.client.gl.VertexBuffer, ключ = инстанс block-list; cutout-тесселяция через встроенный Capture implements VertexConsumer + brm.renderBlock, БЕЗ OccluderGeometryCapturer — тот per-mod; snapshot/restore GL-стейта вокруг draw; флаш кастеров одним glDrawArrays в endPass). Оркестрация/фильтрация/MSM4-математика сохранены.
- ШОВ КАСТЕРОВ ИЗМЕНЁН (против этого пишутся per-mod кастеры): ImmediateOccluderBatch УДАЛЁН, flushCasterImmediate/establishLightMatrices УДАЛЕНЫ; НОВЫЙ org.qualet.irl.light.shadow.RawOccluderBatch implements OccluderBatch с public append(float[] tris) / append(float[],int,int) — POSITION-only треугольники, 3 float/вершина, АБСОЛЮТНЫЕ мировые координаты; emitOccluder-source обязан ((RawOccluderBatch)batch).append(worldTris), НЕ полагаясь на MatrixStack/RenderSystem (INV-1 exempt на этой линии); OccluderBatch-интерфейс (mark/terminateRun) и вся поверхность для ShadowBaker БЕЗ изменений; новый public ShadowRenderer.releaseScratch() зовётся из ShadowBaker.onShadersDisabled (off-heap caster-буфер).
- followUps ядра: спека шва по V3 НЕ стейл (EXEMPTION/INV-1 EXEMPT/rewind-to-mark уже описаны; лишь нюанс: entity-кастер порта описан как box-blob, а он теперь реал-силуэт — деталь потребителя, не контракта); precision TODO — мировые абс. координаты вершин (бандинг вдали от origin, унаследовано от editor-port); ин-гейм гейт обязателен (инварианты шва компайл-таймом не ловятся); TRIPWIRE-слой идёт opaque-путём (minor V3, пренебрежимо).

Редактор merge main->port/1.21.11 — состав (агент E1, Opus, build зелёный):
- Main победил структурно: старое shadow-дерево irlredactor/light/shadow/* УДАЛЕНО (кроме OccluderGeometryCapturer — перенесён), свой IrisShadersState удалён (Tier2 core), CookieArray = main (extends CookieArrayBase), PatcherPanel/RedactorPatcherHost (BUNDLED c rethinkingvoxels) = main, LightDriver/LightScene/LightConfig/LightStore/PlacedLight/AutoLightManager = main, бандл-патчи БАЙТ-В-БАЙТ main (diff пуст), settings.gradle main (includeBuild снят — порт-ветка была на довоенной схеме includeBuild+1.0-obt).
- Порт победил в 1.21.11/port-only: MinecraftClient/Mouse/KeyboardImGuiMixin (deferred GUI после blitToScreen FBO 0; input-рекорды MouseInput/KeyInput/CharInput), LightEditorScreen (no-op applyBlur+renderDarkening), ImGuiInput/ImGuiRuntime (Axiom-guard), ReplayCompat, WorldBlockChangeMixin (isClient()), LightEditorPanel (getCameraPos), fly-camera fix, auto-light counter+nearest-cache, icon.
- Гибриды: GameRendererLightMixin = main-делегат FramePipeline.frame(...) + 1.21.11-таргет renderWorld(RenderTickCounter), tickDelta=getTickProgress(true); LightGuideRenderer = main-логика + порт-рендер (world-события BEFORE_DEBUG_RENDER, POSITION_COLOR_NORMAL_LINE_WIDTH, per-vertex normal+lineWidth, latch-off try/catch); IRLRedactorClient = main-вайринг (ShadowEngine.install + IrlSamplers.register irl_cookieArray) + порт KeyBinding.Category; iris-миксины = main (делегируют в core IrlSamplersBind, который уже 4-арг).
- НОВЫЙ КАСТЕР: src/client/java/org/qualet/irl/light/shadow/{OccluderGeometryCapturer,RedactorEntityCasterSource}.java. Капчерер = git mv из старого дерева (OrderedRenderCommandQueue-имплементация, captureEntityTris/captureCutoutBlockTris, persistent failedEntities-скип), сменён только package. Кастер: база main (collect: LivingEntity/ItemEntity, dist 72, sink.emitFromBox), emitOccluder = ((RawOccluderBatch)batch).append(captureEntityTris(entity,tickDelta)) + per-bake кэш Int2ObjectOpenHashMap<float[]> по entity.getId() со сбросом в collect() (реюз на 6 гранях point-куба; empty-array тоже кэшируется).
- build.gradle: mavenLocal + modClientImplementation/include org.qualet:irl-core:1.1; MC 1.21.11/yarn build.6/fabric 0.141.4/iris 1.10.7/sodium 0.8.7 runtime/replaymod dev-runtime 2.6.26/glsl-transformer 3.0.0-pre3+antlr4/imgui include-блок; Loom 1.14->1.15.5; gradle.properties 1.21.11, jvmargs оставлен -Xmx3G (порт).
- followUps редактора: keybind i18n (category.irl-redactor.main нет в lang — косметика); WorldBlockEntityTickersAccessor мёртвый но required-миксин — при runClient проверить бинд World.blockEntityTickers, иначе удалить; гайды depth-tested (RenderLayers.lines, потерян x-ray — порт-TODO); runtime не гонялся.

РЕВЬЮ 2026-07-08 (3 параллельных adversarial-агента Opus, все PASS, 0 blocker/0 major):
- V1 (потери main-контента): PASS, 0 находок. Merge байт-чист против main (diff-filter=D пуст; 18 файлов остатка = ровно порт-скоуп). Все 11 спот-чеков на уровне контента: патчи 6 шт байт-в-байт (incl. rethinkingvoxels), тонкий GameRendererLightMixin-делегат, CookieArray extends CookieArrayBase, BUNDLED полон, iris-миксины = main (data-driven IrlSamplers-реестр), вайринг ShadowEngine.install + irl_cookieArray, Tier1+Tier2 не откатаны, auto-light = main (counter/nearest-cache в main уже были). 1 nit информационный: гайды depth-tested (порт-TODO, не дефект мержа).
- V2 (потери порт-дельт): PASS, 0 потерь. Все 14 пунктов + (a) raw-GL механика в core подтверждена (glDrawArrays/#version 150/VBO-кэш/RawOccluderBatch, VertexBuffer только в javadoc) и (b) капчерер перенесён байт-в-байт кроме package-строки; iris-миксины сохранили 1.21.11-таргеты (build/updateSampler HEAD, remap=false) и делегируют в 4-арг core-биндинг. 2 info: semver-warning core 1.1 (пре-существующий) + повисший javadoc ImmediateOccluderBatch (ИСПРАВЛЕН этой сессией).
- V3 (core-diff 1.21.4..1.21.11): PASS. Фильтрация: 6 GL-файлов дифф = 1 import-строка каждый, ShadowBaker +4 строки (releaseScratch), оркестрация байт-идентична. ShadowRenderer: дифф против in-world эталона = только пакет/лог-теги/шов/инлайн Capture; snapshot/restore полный; удалённые API отсутствуют whole-tree grep'ом; VBO-lifecycle без утечек (retainBlockVbos чистит оба кэша, releaseScratch освобождает off-heap); casterAccum.clear() в finally -> 6 граней point изолированы. Спека шва НЕ стейл: секции EXEMPTION/INV-1 EXEMPT/rewind-to-mark уже описывают raw-GL бэкенд. 1 minor принят как заметка: TRIPWIRE (новый enum-слой 1.21.11) теперь идёт opaque-путём, а не cutout (на 1.21.4 был getCutout) — тень нити пренебрежима, расширение семантики вне скоупа порта.

Связь: [[project-port-1211]] (июньский порт редактора на 1.21.11 — база порт-ветки), [[project-port-1214]] (образец процесса), [[plan-port-1211-workflow]] (готчи worktree remove), [[plan-irl-core-shadow-extraction]] (шов; спека подтверждена актуальной V3), [[commit-checkpoints]].
