---
name: plan-irl-core-library-extraction
description: "Тир-план выносов в irl-core (ревью 2026-07-04): Tier1+Tier2 РЕАЛИЗОВАНЫ и ЗАКОММИЧЕНЫ 2026-07-05 (core cf4ad94+tag v1.1 / addon 3c3ef3f / editor fa71093; сборка зелёная, ревью 0 мажор — см. блок РЕАЛИЗАЦИЯ); Tier3 спорное не делалось; полный отчёт в _archive"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8a98dfcd-57c0-4460-b4ac-5449f11b29d3
---

Multi-agent расследование 2026-07-04 (Workflow, 83 агента Opus 4.8, 21 кандидат, adversarial-верификация 3 линзами coupling/duplication/value, критик полноты пропусков не нашёл). Полный отчёт с evidence: `_archive/report-irl-core-extraction-review-2026-07-04.md`. Статус: Tier1+Tier2 РЕАЛИЗОВАНЫ и ЗАКОММИЧЕНЫ 2026-07-05 по подтверждению юзера: core main cf4ad94 + tag v1.1, addon master 3c3ef3f (только src; pre-existing D run/photon_v1.3b НЕ закоммичен), editor main fa71093. Push НЕ делался. Tier3 не реализован (только по явному решению юзера).

РЕАЛИЗАЦИЯ 2026-07-05 (workflow 14 агентов + 1 фикс-агент; коммиты см. Статус выше; правило далее — тегировать каждый core publish):
- Tier1+Tier2 выполнены целиком. C19: editor пин 1.0-obt->1.1, core version из gradle.properties irl_core_version=1.1. C21: README ядра приведён к правде (Loom-модуль, mavenLocal-workflow, версия не хардкодится). C1: core org.qualet.irl.light.iris.IrisShadersState (per-mod копии удалены); core build.gradle modCompileOnly maven.modrinth:iris:1.7.2+1.20.4 (НЕ include). C4: core FramePipeline.frame(float, BooleanSupplier shadersDisabled, LightSource collect, Runnable onDormant); оба GameRendererLightMixin = тонкие делегаты; addon onDormant=noop, editor=LightDriver::resetAutoShadowRamp (позиция в dormant-переходе сохранена). C8: core LightMath — cone(outerDeg,innerDeg)->record Cone + normalizeDir с ЯВНЫМ fallback-параметром (4 overload: Vector4f/Vector3f/float[]/Vec3d); 3 cone-сайта + 6 normalizeDir-сайтов переведены, fallback каждого сайта сохранён; латентный NaN-баг gizmo-сайта LightEditorPanel закрыт fallback (0,-1,0) — единственное намеренное поведенческое изменение. C14: Shaderpacks.norm/packMatchesTarget/outputName(IrlPatch,String,boolean) — заодно починен шипнутый editor-баг потери +DOF-суффикса. C12: PatchResult.Outcome (12 значений), ставится в месте ошибки (PatchEngine.addError+firstErrorOutcome, IrlPatchApplier OK/CONTRACT_MISMATCH/BAD_SOURCE/IO_ERROR); оба UI на switch(outcome), тексты не менялись. C2: core CookieArrayBase (RES=512, uploadLayer c PBO/UNPACK-guard вербатим, static decode, depth параметром конструктора; getGlTextureId/delete остались static-фасадами модов — Java запрещает static override); per-mod политика на месте (addon 32+LRU+failed-set, editor 16); editor-баг «кэш -1 навсегда при полном массиве» починен. C3: core IrlSamplers (LinkedHashMap, порядок регистрации = контракт GLSL; SamplerConsumer-визитор) + iris.IrlSamplersBind.bindAll/tryRebind; 7 сэмплеров: 6 core-владельцев в static{}, irl_cookieArray из client-init каждого мода; оба ProgramSamplersBuilderMixin и SamplerBindingCubeArrayMixin схлопнуты до 1 вызова, остаются per-mod, mixins.json в ядре НЕТ. C5: editor WorldBlockEntityTickersAccessor удалён (файл+mixins.json). C18: ShadowConfig.builder() именованный c валидацией build(); addon IrliteShadowConfig -> static INSTANCE holder (caller IrliteClient поправлен), editor LightConfig.SHADOW через builder. W-a: LightStore.Dto слит в PlacedLight (transient id/autoShadowEligible, Gson InstanceCreator для честного конструктора, guard-ы legacy-JSON; формат персиста сверен с реальным файлом сцены) + фикс-раунд после ревью: толерантный JsonDeserializer для PlacedLight.Type (ровно "SPOT"->SPOT, иначе POINT — старая семантика Dto; запись name() не менялась).
- Сборка зелёная с первого прогона: core publishToMavenLocal -> addon clean build -Pmc=1.20.4 --refresh-dependencies -> editor clean build; ноль конфликтов 8 параллельных стримов; editor после фикс-раунда пересобран.
- Ревью (3 opus-линзы + adversarial): 0 blocker / 0 major. Nit-остатки (НЕ чинились, осознанно): IrlSamplers.glTargetFor пока без потребителей (оставлен — API из плана); FramePipeline.dormant + слот irl_cookieArray теперь общие static в JiJ-core — коллизия ТОЛЬКО при будущем in-JVM co-load двух модов (Flashback-сценарий, согласуется с известным «static-синглтоны ок при текущем деплое»).
- ПОПРАВКИ ФАКТОВ ПЛАНА по реальному коду: cookieRotation-дрейф из C7 УСТАРЕЛ — единицы сквозно согласованы с коммита 7d62612 (UI DEGREES <-> LightSync.pull/push <-> PlacedLight RADIANS <-> движок), W-b правки не потребовал; cone-cos копий 3, а не 4; кламп конуса = min УГЛОВ до cos (не min косинусов, как было сформулировано); Java модов лежит в src/client/java (не src/main/java).
- Ин-гейм НЕ проверялось (статическая реализация + компиляция): кадр обоих модов, тени/сэмплеры/cookie, патчер-UI исходы, +DOF-имя редактора, гайды спота при вырожденном dir, загрузка сохранённых сцен редактора.

ВЕРДИКТ: граница core/моды спроектирована хорошо (патчер/свет-SSBO/тени-оркестрация только в ядре, швы PatcherHost+ShadowCasterSource образцовые, ядро реально BBS/ImGui/Iris-free — grep пуст). Но общих файлов аддон<->редактор ВОСЕМЬ (не один CookieArray): CookieArray, IrisShadersState (байт-идентичен), LightGuideRenderer (осознанный форк, НЕ дубль), GameRendererLightMixin (>90% общего), WorldBlockChangeMixin, WorldBlockEntityTickersAccessor (у редактора мёртв), ProgramSamplersBuilderMixin, SamplerBindingCubeArrayMixin.

ГЕЙТЫ МЕЖДУ КАНДИДАТАМИ: C19 гейтит уптейк редактором ВСЕХ Java/enum-выносов (C1,C3,C4,C8,C12,C14,C18); C1 гейтит helper-часть C3 (Iris-dep в ядро вводится один раз, батчить C1+C3); C16 предусловие C11; C9+C1 предусловие C10.

TIER 1 — делать, очевидная выгода, низкий риск (порядок = порядок реализации):
1. C19 версионирование ядра, ПЕРВЫМ, effort S, риск нулевой. Факт: редактор пинит irl-core:1.0-obt (irlights/build.gradle:61-62), аддон 1.1 (bbs-irlights-addon/build.gradle:109-110), core hardcode version='1.1' (irl-core/build.gradle:19); jar 1.0-obt в mavenLocal (проверено unzip) без 4 классов F1/F2 (Point/SpotShadowEvsm+Pyramid), а editor ProgramSamplersBuilderMixin:10-13 их импортирует => компиляция редактора против 1.0-obt должна падать; git tags ядра пусты. Делать: (a) бамп пина 1.0-obt->1.1 в irlights/build.gradle; (b) общий irl_core_version property в irl-core/gradle.properties (сейчас там только jvmargs); (c) git-tag per publish. НЕ делать --refresh-dependencies обвязку (её нет ни у кого, чинится a+b). Безопасно пока оба потребителя на MC 1.20.4 (сейчас так).
2. C21 README/докстринги ядра, S. irl-core/README.md:7,24 "zero Minecraft, zero Loom" ложь (build.gradle:13-14 loom, :46-47 minecraft 1.20.4+yarn, 8 shadow-классов с net.minecraft); :41-51 includeBuild мёртв (оба settings.gradle ретайрят composite); badge :11 = 1.0-obt при version 1.1 — badge не хардкодить, завязан на C19. build-trilogy.ps1 untracked (BBS-root не git) — README приоритетнее.
3. C1 IrisShadersState -> core, S. Тело строк 2-48 байт-идентично между модами (разница только package + EOL). Вынести вербатим в org.qualet.irl.light.iris.IrisShadersState (пакет .iris изолирует Iris-код); добавить modCompileOnly Iris в core build.gradle (сейчас :43-53 без Iris) — одноразовая цена, батчить с C3. Потребители правки import: GameRendererLightMixin x2, editor AutoLightManager.java:168.
4. C4 FramePipeline -> core, S. Два GameRendererLightMixin совпадают вербатим кроме package/imports/editor-only LightDriver.resetAutoShadowRamp (editor:46-48)/collect-таргета (addon:63 vs editor:66); общее тело ~40/44 строк. API: FramePipeline.frame(float tickDelta, BooleanSupplier shadersDisabled, LightSource source, Runnable onDormant); тело = dormant-gate -> clear/uploadEmpty/onDormant; cameraPos/forward; collect callback сигнатуры (ClientWorld,Vec3d,float) — дословно совпадают у обоих; entityRenderDispatcher.configure; ShadowBaker.bake; LightRegistry.flush. КРИТИЧНО: shadersDisabled держать BooleanSupplier, НЕ звать core IrisShadersState напрямую (иначе Iris-dep протекает туда, где не нужен). @Mixin(GameRenderer) остаётся per-mod тонким делегатом.
5. C8 LightMath -> core, реалистично M. Две части. (a) coneCosHalf(deg)=cos(rad(deg*0.5))+inner=min(inner,outer) — pure, выносить сразу; копий 4: LightCollector.emitSpot:370-382, LightDriver.emitSpot:130-149, SpotlightFormRenderer.java:91-93, +1 у финдеров. (b) normalizeDir — ПОВЕДЕНЧЕСКИЙ: 6 копий с ТРЕМЯ разными fallback ((0,0,1) в драйверах; (0,-1,0) в LightGuideRenderer.java:123 и LightEditorPanel.orientationFromDir:744-748; fallback ОТСУТСТВУЕТ в LightEditorPanel:725-730 = латентный баг); унификация меняет визуальный дефолт конуса при вырожденном dir — выносить только с ин-гейм re-verify гайдов.

TIER 2 — выгодно, есть блокеры/оговорки (все Java-пункты после C19):
1. C14 helpers -> core Shaderpacks, S. norm() и packMatchesTarget() байт-идентичны (diff rc=0); outputName дрейфнул как ШИПНУТЫЙ БАГ: editor PatcherPanel.java:363 сигнатура без IrlPatch => DoF-combo паки теряют +DOF-суффикс, который аддон добавляет (UIPatcherSection.java:301). API: static outputName(IrlPatch, String packName, boolean createNew) — параметр createNew нормализует разницу (editor instance+ImBoolean vs addon static+поле). Попутно чинит +DOF-баг редактора.
2. C12 PatchResult.Outcome enum, S. Оба UI дублируют string-sniffing по подстрокам англ. summary (UIPatcherSection.java:336-358, PatcherPanel.java:430-452 — те же подстроки, тот же порядок). Outcome ставить В МЕСТЕ ошибки (PatchEngine.java:98/109/142/151 -> TARGET_NOT_FOUND/READ_ERROR/ANCHOR_AMBIGUOUS/ANCHOR_NOT_FOUND; ALREADY_PATCHED у источника), не парсить агрегат report():221; итого ~10 значений. PatchException private nested в IrlPatchApplier:309-315 — наружу не летит, не блокер.
3. C2 CookieArrayBase -> core, M, SURGICAL-вариант (полный перенос отклонён). В core: CookieArrayBase{RES=512, getGlTextureId, ensureInit, delete, protected uploadLayer(ByteBuffer,int) с PBO/UNPACK-guard} + pure decode() helper. Upload-код байт-идентичен (addon:200-236 vs editor:193-231), guard чинил реальный EXCEPTION_ACCESS_VIOLATION. Per-mod остаётся resolve/cache/политика: у копий РАЗНОЕ поведение (addon MAX_LAYERS=32+LRU+failed-set vs editor 16 без эвикции) — не хардкодить cap/eviction в ядро. Латентный баг редактора (кэширует -1 навсегда при полном массиве) — починить попутно. BBS Link/BBSMod в ядро не тащить.
4. C3 Iris sampler-bind, два уровня. (a) Registry-половина S, можно сразу после C19: IrlSamplers{register(name,IntSupplier,glTarget), forEach, glTargetFor} — только java.util.function+int, БЕЗ Iris-типов; устраняет 7 хардкод-веток addDynamicSampler (все 7 вызовов идентичны между модами; SamplerBindingCubeArrayMixin различается ровно 1 строкой import). (b) Helper-половина (bindAll/tryRebind, типы ProgramSamplers.Builder/IrisRenderSystem) — после C1 (Iris-dep уже введён). Full-lift миксинов в core ОТКЛОНЁН (требует первого mixins.json в ядре).
5. C5 safe-вариант, S: удалить МЁРТВЫЙ editor WorldBlockEntityTickersAccessor (зарегистрирован irl-redactor.client.mixins.json:12, использований в irlights/src ноль). Вынос тела WorldBlockChangeMixin в core отложить — требует первого mixins.json в ядре; сделать райдером, если mixins.json появится по более сильной причине.
6. C18 ShadowConfig.of(...) фабрика, S: оба мода пишут одинаковый shim из 5 геттеров-делегатов (IrliteShadowConfig.java:19-23, LightConfig.java:16-23). Только райдером к уже идущему core-релизу (код ~10 строк, сам по себе bump не окупает); НЕ позиционная сигнатура из 5 supplier'ов (footgun) — именованный builder/named-args.
7. Локальные win-ы БЕЗ ядра, не гейтятся ничем, можно в любой момент: (a) слить LightStore.Dto в PlacedLight локально в репо редактора; (b) фикс единиц cookieRotation (form DEGREES vs PlacedLight RADIANS — подтверждённый дрейф).

TIER 3 — спорно, только по явному решению юзера (value-линза у всех: цена>выгоды):
1. C16 (поглощает C15) единый irlite_lights.glsl шаблон в core, L. Дрейф тела реален: addon solas 1413 стр vs editor 1020, diff 697 — editor GLSL без F1/F2/VL_NOISE/богатого OUTLINE, при этом editor Java-рантайм УЖЕ биндит весь стек (отстало только GLSL-тело, НЕ lightweight-профиль). Блокеры: DSL без @include (IrlPatchParser.java:125-130 инлайнит вербатим); у editor НОЛЬ gen-скриптов (у addon 6); расхождение в самой shading-математике. СНАЧАЛА решить process-вопрос: дать редактору gen-пайплайн ИЛИ ре-синк editor .irlights из addon-вывода — возможно дешевле вовсе без core-API. SSBO-struct сам по себе не дрейфует (17 байт-идентичных копий md5 057be7a).
2. C11 единый источник .irlights в core, M: только ПОСЛЕ C16 и решения по намеренной заморозке editor-патчей (fix-only-main директива). Дрейф всех 6 пар на десятки КБ (photon 88616 vs 48714B); iterationrp gitignored в обоих (addon .gitignore:134, editor :30).
3. C7 полный LightParams value-object, M: формы аддона ОБЯЗАНЫ остаться BBS Value* (кейфреймы) => дубль полей не исчезает, добавляется маппинг-слой + per-frame аллокация в горячем цикле до 2048; дешёвая часть уже в Tier 2 п.7.
4. C17 ShadowSettingSpec, S: defaults дублированы трижды, но MED vs MEDIUM дрейф лейблов НАМЕРЕННЫЙ (LightEditorPanel:579 segW=avail/4, MEDIUM не влезает); если делать — только defaults-часть райдером.
5. C9 BlockLightDefs -> core (S) и C10 AutoLightScanner -> core (M-L): только если аддон реально начнёт auto-light — сейчас второго потребителя нет (в аддоне ноль следов auto-light), AutoLightManager static-синглтон ~20 полей требует рерайта в instance.
6. C20 формализация per-MC split, M: премиса ложна — editor port/1.21.x ветки самодостаточны (своё 10-файловое shadow-дерево org.qualet.irlredactor.light.shadow), core потребляют только для MC-нейтрального; параметризация ухудшила бы.

ОТВЕРГНУТО (не делать): C6 GuideGeometry — два LightGuideRenderer осознанный форк (TRIANGLES/BBS Draw.fillBoxTo/48 сегментов/local vs DEBUG_LINES/WorldRenderEvents.LAST/20/world), общего ~1 строка cone-trig; C13 AbstractIrisPatcherHost — 33 байт-идентичные строки не окупают первый Iris-dep, прокалывающий документированно Iris-free шов патчера (PatcherHost.java:11).

ЗДОРОВЬЕ ЯДРА (ревью-находки вне тиров, для отдельного API-рефакторинга): LightRegistry.registerSpot 24 позиционных float (LightRegistry.java:78), addSpot 23 (LightBuffer.java:99) — кандидат на builder; имя irlite захардкожено в нейтральном слое (PatchEngine.MARKER_FILE irlite_patched.txt :30, @irlite DSL, sysprop irlite.profileShadows, лог-теги) — трогать только при реальном ребрендинге (ломает marker-совместимость пропатченных паков); стейл-комменты контракта (LightBuffer.java:38 "80B/164KB" при реальных 96B/192KB, LightRegistry.java:11 и editor LightConfig.java:72 "256" при MAX_LIGHTS 2048); всё ядро static-синглтоны — ок при текущем деплое (разные JVM, JiJ-копии), критично при будущем in-JVM co-load (Flashback); CONTRACT_VERSION=1 (IrlPatch.java:18) НЕ привязан к std430-layout — смена порядка полей LightBuffer не бампит версию, golden-теста Java<->GLSL нет; LightRegistry.slot() линейный O(n) по identity на регистрацию (:99-119) => O(n^2) на кадр при 2048; PatchLibrary.extracted глобальный latch (:23) подтверждён, но ЛАТЕНТНЫЙ (editor breaks bbs:* + JiJ => два хоста в одной JVM невозможны; при 3-м хосте -> Set<Path>).

ПОПРАВКИ ПАМЯТИ (главные, полный список 33 шт в отчёте): "единственный шов = CookieArray" НЕВЕРНО (8 общих файлов); "CookieArray байт-идентичен" НЕВЕРНО (поведенчески разошлись); ".irlights идентичны addon/editor" НЕВЕРНО (все 6 пар, дрейф частично намеренный fix-only-main); "оба мода на одной версии ядра" НЕВЕРНО (1.1 vs 1.0-obt); auto-block-lights реализован ТОЛЬКО в редакторе (вопреки project-auto-block-lights); tools/ ядра = один retired stub verify-shadow-lockstep.py, второго патчера нет; SSBO 96B/6xvec4/binding7/MAX_LIGHTS 2048 ПОДТВЕРЖДЕНО; оркестрация теней в core + шов ShadowCasterSource ПОДТВЕРЖДЕНО.

НЕ ПОКРЫТО: ин-гейм верификация (расследование статическое; C8b и любой GLSL-вынос требуют ин-гейм re-verify); глубокий API-рефакторинг ядра (builder, де-статизация) — отдельная тема.

Связь: [[project-irl-sync-strategy]] (устаревшая карта дрейфа, скорректирована), [[reference-edit-routing-by-area]], [[patcher]], [[addon-light-buffer-ssbo]], [[project-auto-block-lights]].
