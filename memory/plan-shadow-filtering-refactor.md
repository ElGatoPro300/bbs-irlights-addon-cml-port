---
name: plan-shadow-filtering-refactor
description: "Рефакторинг фильтрации теней: point-канал ЗАВЕРШЁН на MSM4-Hamburger + seamless cube-view (чекпоинт 2026-07-03: core 6668f22, addon e4935a0, redactor 57a7dfd) — все ин-ворлд критерии PASS (линии/квадрат/кольцо/рамки-Softness0/перекрестье/контакт/contact-hardening план A); открыто: overlay-перф Ф2a, FPS-замер, тираж на 6 паков по команде."
metadata: 
  node_type: memory
  type: project
  originSessionId: a102893e-2771-4573-937c-afa176631989
---

Исследование рефакторинга фильтрации теней (PCSS -> альтернативы), 2026-07-01. Метод: workflow 4 аудита (GLSL/Java-бейк/база проблем/нативные паки) + 4 web-рисёрча (префильтр VSM-EVSM-MSM / современный PCSS / экзотика / бейк-гибриды) + 3 судьи (качество/перф/миграция). Полные отчёты: `_archive/shadow-refactor-research-2026-07-01/`.

IN-WORLD PASS 2026-07-02 (photon_v1.3b_IRLights): юзер подтвердил оба видимых дискриминатора по эталонной визуализации — (1) края point-теней сглажены gather'ом (лесенка осталась — это разрешение карты, но ступеньки с плавными переходами), (2) узор зерна в полутени = ровный IGN без сгустков. Значит весь файл, вкл. Ф1a-пирамиду, активен на GPU. НЕ замерено: FPS-выигрыш early-out/пирамиды (нужен GPU-bound замер), судьба «зернистого квадрата» на высокой Softness. Известный шум лога: «#endif without #if» 2x на каждую загрузку photon-пака (null:0:0, INFO сканера опций Iris, НЕ фатально — баланс директив в файле 42/42 идеален; до правок photon-загрузок в логах нет, так что pre-existing или нет — неизвестно).

СТАТУС Ф0 (2026-07-01, та же сессия): РЕАЛИЗОВАНА в Shadres/Modification/Photon irlite_lights.glsl + BSL outline-фикс; синк в run/shaderpacks/{photon_v1.3b_IRLights, BSL_IRLights}; состязательное ревью 3 линзами пройдено (0 блокеров), 5 фиксов ревью вкатаны: (1) VL-stride через счётчик дошедших шагов, не номер шага (continue-шаги не съедают слоты; в BSL-копии старая слабость осталась — выровнять при тираже); (2) IGN-декорреляция PCF честная — переставленные коэффициенты градиента, сигнатура irlite_rotationPhi(int seq); (3) NaN-гард в point-шарпене (max(minPen,1e-6)); (4) early-out проба расстелена по радиусу перестановкой (i*13)%N (13 взаимно просто с 10/18/28/40); (5) LOD-гард material.roughness>0.3 (глянец не берёт fast-путь). Новые дефайны: IRLITE_SHADOW_IGN / _EARLY_OUT / _LOD (+_LOD_THRESHOLD 0.01) / _SHARPEN / IRLITE_POINT_GATHER / IRLITE_VL_SHADOW_STRIDE 2 — не в settings-экране. ВАЖНО (feedback юзера 2026-07-02): IGN выключен по вкусу — sin-hash «плёночное зерно» выглядит ЛУЧШЕ упорядоченной «ткани» IGN (после Ф2 шум остался только в контактных полутенях); IRLITE_SHADOW_IGN держать закомментированным при тираже, остальные default ON. Бюджеты тапов НЕ резались (паки живут на 3-6/9-16 только за счёт temporal под TAA — у нас spatial-only инвариант). textureGather(samplerCubeArray) подтверждён core GLSL 4.00; все 6 хост-программ Photon = #version 400 compatibility.

WATCHLIST in-world проверки Ф0 (из ревью): кольцо/бандинг на внешней кромке широкой полутени (early-out проба, статичная граница без temporal); ободок у края конуса спота (кламп-политика тайла, pile-up краевого текселя); 1-px нитка на рёбрах куба point-тени (gather у шва seamless, историч. AMD-драйверы); LOD-шов у границы радиуса тусклых ламп (порог 0.01 до 5x baseline — ослабить до 0.003-0.005 если виден); тени окклюдеров вплотную к лампе стали резче (point penumbra кап lightSize, паритет со spot — поднять кап до ~4x lightSize если сцены «объект у лампы» важны).

СТАТУС Ф1a (2026-07-02): РЕАЛИЗОВАНА, ревью 3 линзами пройдено (2 major пойманы и закрыты). Java irl-core: новый SpotShadowPyramid (compute #430, RG32F, base=atlas/2, levels=log2(TILE_SIZE), лоды 0..N-1 = вплоть до 1 текселя/тайл; БАТЧ markDirty(tile)+flushDirty() раз на бейк после спот-цикла — 9 glGet и ~levels барьеров на КАДР, не на тайл; стейт save/restore: program/texture unit/image unit 0, текстур-байнды через GlStateManager; провал компиляции -> inert + GLSL-гард). ShadowBaker: markDirty после no-cache бейка / dirty static бейка / overlay-секции (каждый кадр), flushDirty перед point-циклом. SpotlightDepthAtlas.delete -> пирамида delete; _deleteTexture вместо raw glDeleteTextures (протухший кэш GlStateManager при name-reuse — pre-existing баг закрыт парой). GLSL Photon: IRLITE_SHADOW_PYRAMID early-out (lod с текселем >= 2x футпринта, 2x2 texelFetch с клампом в mip-регион тайла; cmpDepth<=min -> 1.0 доказанно == blockerCount==0; cmpDepth>max && max>0 -> 0.0 — гард max>0 обязателен: texture 0 при провале compute/version-skew иначе даёт чёрные тени) + IRLITE_SHADOW_ADAPTIVE (квантованные тапы NARROW/MID/full по ширине пенумбры в текселях, пороги 3/8; perm=(pcfN%6==0)?5:13 — 13 тождественна mod 6/12). Миксины: irl_spotShadowPyramid добавлен в ОБА мода (аддон + редактор — ОБЯЗАТЕЛЬНО лочстепом, патчи общие). VRAM пирамиды ~2/3 live-атласа (MEDIUM ~43MiB, ULTRA ~683MiB; RG16F НЕЛЬЗЯ — round-to-nearest ломает консервативность). Инвариант: регионы небейканных тайлов = мусор, сэмплинг легален только при vlParams.w>=0. Open-note: Java строит пирамиду безусловно даже при выключенном дефайне пака (mod-side гейт в ShadowConfig — кандидат).

СТАТУС Ф1b (2026-07-02): IN-WORLD PASS (юзер: point-тени идентичны, артефактов нет); коммит core 0f6d644. РЕАЛИЗОВАНА, ревью 3 линзами (1 блокер закрыт). Java irl-core: PointShadowPyramid — face-major GL_TEXTURE_2D_ARRAY RG32F (base=face/2, 96 слоёв layer=slot*6+face, levels=log2(FACE_SIZE)); ДВЕ compute-программы: CUBE_SRC (depth samplerCubeArray -> lod0, реконструкция направления обратной таблицей faceDir — доказана взаимная обратность с irlite_cubeFaceUV; чтение в центрах текселей = точный тексель даже при LINEAR, доказано вкл. краевые тексели при seamless) и MIP_SRC (texelFetch 2D-array); батч markDirty/flushDirty после point-цикла, layered image binding, dispatch z=6 (все грани слота), cube/2D-array байнды raw (GlStateManager их не трекает — только 2D). ShadowBaker: markDirty в 3 сайтах point-цикла (no-cache/dirty-static/overlay каждый кадр — partial copyMask ребилдит все 6 граней, консервативно ок); PointShadowArray.delete -> каскад. GLSL: early-out c edge-гардом (футпринт у границы грани -> полный PCSS: face-local пирамида не видит соседнюю грань), uvHalf = footWorld/max(zPersp-footWorld,1e-4) (консервативный знаменатель — бонд 1-го порядка эродирует у краёв), footTex В DEPTH-ТЕКСЕЛЯХ = uvHalf*faceRes (БЛОКЕР ревью: base-тексели с формулой лода спота = октава недопокрытия, до 2x при footTex чуть ниже степени двойки); textureSize-гейт base==face/2 (unbound array-сэмплер читает чужой 2D_ARRAY юнита, напр. cookie — pyrMax>0 не ловит ненулевой мусор); хелпер irlite_cubeFaceUV (спек-таблица, gather-тап отрефакторен). Миксины 4 файлов оба мода: регистрация 2D + rebind GL_TEXTURE_2D_ARRAY (паттерн cookie). Доказано ревью: |d(uv01)| <= 0.72*footWorld/zPersp при пройденном гарде (запас 28%); pow2 FACE_SIZE обязателен (коммент в setFaceSize). VRAM: point-пирамида ~2/3 live-куба (MEDIUM ~67MiB, ULTRA ~1.02GiB) — док в IRLShadowQuality обновлён.

СТАТУС Ф2a (2026-07-02): РЕАЛИЗОВАНА + ревью 3 линзами, 3 major математики закрыты; ждёт in-world ИНТЕРЬЕРНОГО теста (light bleeding = худший кейс Чебышёва) + свипа Softness через порог веток. Java irl-core: SpotShadowEvsm — EVSM4 RGBA32F base=atlas/2 + mips (levels=log2(TILE), trilinear сэмплер), temp (TILE/2)^2; 3 compute: CONVERT (depth -> ЛИНЕАРИЗОВАННЫЙ lz=(dist-near)/(far-near) -> warp wp=exp(42 lz), wn=-exp(-8 lz); усреднение моментов 2x2 = точный префильтр; per-tile uniform far через markDirty(tile, range) — ревью: warp на perspective z01 деградирует к plain-VSM на дали + floor растёт квадратично), BLUR (separable 1-4-6-4-1, кламп в тайл, srcLod), MIP (2x2 avg); КАЖДЫЙ mip ре-блюрится после даунсэмпла (ревью: голая avg-цепочка НЕ удваивает ширину фильтра на лод — sigma~2^L/sqrt(3) вместо 2*2^L, широкие полутени были бы в 2-3.5x резче PCF-эквивалента; с пер-лод блюром sigma~2.3*2^L, расхождение ~15%); батч по паттерну пирамиды. Сырая depth-карта живёт: blocker search/контакт-PCF/VL нетронуты. GLSL: ветка после blocker search, penDepthTex>=4 && size-гейт (evsmRes*2==atlasSize.x) -> lod=log2(pen*0.25), UV-кламп полутекселя ГРУБОГО мипа (ceil(lod), min 0.125=полтайла — tMin<=tMax всегда), 1 textureLod + двойной Чебышёв (variance floor = (производная warp * 1e-4)^2; формула irlite_chebyshev корректна для обеих компонент — оба warp возрастающие), min, linstep bleed 0.10 (ревью: 0.25 ломает бесшовность — Чебышёв ТОЧЕН для двухплоскостного распределения, EVSM==PCF до linstep; скачок 0.17 + спекл на изолинии порога), БЛЕНД-ЗОНА pen 4..6 текселей = smoothstep-mix PCF<->EVSM (early-out проба отключена в зоне — mix требует полного цикла). Миксины обоих модов: irl_spotEvsm (plain 2D). ОТКРЫТЫЕ ХВОСТЫ Ф2a: (1) overlay-путь = convert+blur+mips каждый кадр на тайл (~40-60MiB трафика/тайл MEDIUM, HIGH с 16 overlay-спотами ~8-12ms — неприемлемо; варианты: CPU max-penumbra cull по bulbSize, полноатласный temp для батч-барьеров (+64MiB), НЕ джиттер — шов веток); (2) формат-лестница fp16 на LOW/MED; (3) CN=8 можно поднимать до 42 без переполнений если дальний bleeding проявится; (4) VL всё ещё на raw depth (ESM-канал = будущая опция).

СТАТУС Ф2b (2026-07-02): РЕАЛИЗОВАНА + ревью (адаптация против эталонов Ф1b/Ф2a чистая; 2 major закрыты: (1) гард irlite_cubeFaceUV расширен на IRLITE_SHADOW_PREFILTER — A/B-комбо PREFILTER-only не компилилось; (2) size-гейт слеп к пирамиде того же размера face/2 — добавлен пост-фетч validity `mm.z < 0.0` в ОБЕ ветки: E[wn] строго отрицателен у настоящего EVSM, у RG-пирамиды/куки .z=0). PointShadowEvsm: face-major 2D-array RGBA32F base=face/2, CONVERT линеаризует dominant-axis depth per-slot far, blur через 6-слойный temp (dispatch z=6), mip+ре-блюр; GLSL point-ветка: ИТЕРАЦИИ ПО IN-WORLD 2026-07-02 — (1) порог по penumbra УБРАН для point (EVSM всегда при валидной карте): скрин юзера показал старый «зернистый квадрат» = acne боковых граней куба на PCF-пути (Softness дефолт 0.10 не пускал EVSM); Чебышёв без бинарного теста глотает acne по построению; (2) edge-гард УБРАН (при большом bulbSize футпринт «пересекал» границу везде -> вся грань падала в PCF, квадрат возвращался) — остался только UV-кламп фетча; принятая цена = мягкая face-local ошибка блюра в узких полосах у рёбер вместо зерна; follow-up если полосы проявятся = сферически-корректный блюр в CONVERT (сэмплы куба вместо face-local separable). Spot-ветка не тронута (порог 3 текселя + бленд-зона остались — юзер подтвердил чистоту). Также: adaptive NARROW/MID подняты (Q2: 10/16), IGN выключен по вкусу юзера (sin-hash дефолт). Ф2a in-world PASS: зернистый квадрат на спотах МЁРТВ (юзер подтвердил). ЧЕКПОИНТ 2026-07-02: Ф2b закоммичена — core 9783eee (PointShadowEvsm + ShadowBaker/PointShadowArray/IRLShadowQuality), redactor 7b37cd2 (биндинг irl_pointEvsm, лочстеп); addon-половина была закоммичена ранее. Актуальная рабочая копия GLSL = дев-клиент run\shaderpacks\photon_v1.3b_IRLights\shaders\include\irlite\irlite_lights.glsl (структура Shadres дрейфанула — путей Original/Modification из sync-workflow больше нет, правки идут напрямую в run-копию с hot-reload).

ИН-ГЕЙМ ТЕСТЫ Ф2 (2026-07-02, юзер): T1 интерьерный bleeding PASS; T2 свип Softness спот (шов веток) PASS; T3 рёбра куба point FAIL — предсказанные полосы вдоль ±45°, проявляются от Softness ~0.10 до максимума (шире полутень -> грубее мип -> шире полоса кламп-ошибки). Фикс реализован (НЕ закоммичен): cube-aware тапы в BLUR_SRC PointShadowEvsm — off-face тап -> faceDir(незаклампленный st) -> обратная доминантно-осевая таблица -> texelFetch соседней грани (инверсия сверена по всем 6 граням); работает на H, V и пер-мип ре-блюрах (temp держит все 6 граней). CONVERT/MIP не трогались (2x2 строго внутри грани). Джавадок класса обновлён (стерто устаревшее «edge-guard, no spherical blur needed»). Ин-ворлд проверка pending.

BONUS-BAG (open): перекрестье размытых теней 2 окклюдеров ОДНОЙ лампы светлее ожидаемого = penumbra-overlap bleeding Чебышёва (бимодальное распределение; min(pos,neg) структурно даёт ~min(1-p1,1-p2) вместо 1-p1-p2). Крутилки ИСЧЕРПАНЫ без эффекта по оценке юзера: IRLITE_EVSM_BLEED_POINT введён (0.18->0.25, point-only, спот не тронут) + CN neg warp 8->14 лочстепом (Java CONVERT + GLSL точка; 14 = fp32-безопасный потолок, выше — прецизионное acne на дальних lz; клейм памяти «до 42» проверял только overflow). Реальный фикс = MSM (Hamburger 4-моментный) для point — РЕАЛИЗОВАН 2026-07-02 по команде юзера (in-world pending, НЕ закоммичен): CONVERT хранит (lz, lz2, -lz3, lz4) — третий момент НЕГИРОВАН как validity-маркер (гейт mm.z<0 сохранён: мусор/пирамида/куки читаются >=0, солвер флипает знак); GLSL irlite_msmHamburger (Peters&Klein, момент-биас 3e-7 fp32, гарды на делители) вместо min(2x Чебышёв); новый IRLITE_MSM_DEPTH_BIAS 3e-4 (линейный биас ресивера); BLEED_POINT откачен 0.25->0.10 (MSM течёт меньше, 0.25 был EVSM-костылём); спот остаётся на EVSM (irlite_chebyshev жив для спота); класс/семплер сохранили EVSM-имена (mixin-чурн в 2 модах не нужен — биндинг не менялся). Blur/mip/temp нетронуты (моменты линейны). Риск-вахта MSM: peter-panning от depth-bias, светоутечки у контактов, acne на боковых гранях (bias теперь линейный а не warp-производная).

ОХОТА НА ПОЛОСЫ ±45 (2026-07-02, продолжение): фиксы №1 (cube-aware блюр, байт-верифицирован в ремапе) и №2 (де-плато бленд сэмплинга: irlite_cubeFaceDir хелпер + второй фетч на соседней грани, mix по глубине проникновения в кламп-маржу) ДОСТАВЛЕНЫ, но юзер репортит полосы живы. Пирамидный edge-гард ПРИСУТСТВУЕТ (строка ~570: футпринт пересекает грань -> skip early-out) — но граница skip-полосы шириной uvHalf~lightSize/zPersp вдоль ±45 = кандидат в источник (мисматч early-out vs полный путь). ДИАГНОСТИКА ЗАПУЩЕНА: тест A = IRLITE_SHADOW_PYRAMID закомментирован (DIAG-метка в дефайне, ВЕРНУТЬ после) — полосы исчезли => мисматч на границе гарда пирамиды; остались => корень в EVSM-фетче (следующее сечение: откат бленда, кап lodP).

ЗАКРЫТИЕ ТОЧКИ (2026-07-03, чекпоинт core 6668f22 / addon e4935a0 / redactor 57a7dfd): все критерии PASS от юзера — квадрат/линии45/кольцо/рамки-Softness0 НЕТУ, перекрестье и контакт чисты. Финальный стек point-канала: MSM4-Hamburger (moment bias 1e-4 дефайн, depth bias 3e-4) + hardware seamless CUBE_MAP_ARRAY texture view (канон для префильтр-текстур) + cube-aware блюр-тапы + E4 (bias от zPersp, тоже в VL) + slope-aware tapMargin в поиске + plane-margin в пирамиде + slope-scaled normal offset (0.25 texel/ndl, старый 1.5texel+0.05 давал отрыв контакта) + LOD fast-путь = мягкий MSM-фетч (эвристика 0.5*lightSize; порог 0.003) + contact-weighted оценка блокера (план A: вес 1/max(refDist-d, texelWorld) — резкий корень, мягкий хвост, фото-3 look). План B (контактная PCF-ветка ниже пре-блюра, спот-паттерн, гейт на blockerCount>0) ОПРОБОВАН И ОТКАЧЕН по решению юзера 2026-07-03 — код удалён, при возврате к теме см. этот абзац (дизайн: LO/HI 3/6 текселей, smoothstep-бленд через irlPreVisP/irlPreWP, рычаг LO=0). Пак-GLSL теперь ТРЕКАЕТСЯ в addon-репо (git add -f run/shaderpacks/photon_v1.3b_IRLights/.../irlite_lights.glsl). Спот-канал остался на EVSM+Чебышёв — перевод на MSM/такие же root-фиксы = кандидат тиража.

РАУНД 4 IN-WORLD PASS (2026-07-02, юзер: «не могу поверить — линии 45 пропали»): cube-view = ПОБЕДА над ±45-линиями, канон-решение для любых face-major префильтр-текстур. РАУНД 5 PASS: квадрат фронтальной грани мёртв (slope-маржа в поиске блокеров сработала). РАУНД 6 (in-world pending): юзер увидел круглую границу размытия раньше конца радиуса = предсказанный вотчлистом Ф0 LOD-шов (attenuation-LOD fast=hard binary vs мягкий MSM, изолиния = круг); фикс = IRLITE_SHADOW_LOD_THRESHOLD 0.01 -> 0.003 И СРАЗУ план B (юзер прислал схему — кольцо явно выражено): fast-путь под PREFILTER = 1 MSM-фетч (эвристика penF=0.5*lightSize, floor 1.5 texel, лод от 2*refDist/faceRes; validity+size-гейт, фолбэк на старый gather) — мягкость структурно до конца радиуса, кольца нет как класса; бинарный тап остался только фолбэком/без PREFILTER. Пирамида ВОЗВРАЩЕНА (диагностика окончена) — финальный прогон: линии/квадрат/кольцо/перекрестье/контакт. РАУНД 5 (in-world pending): остаточный ОЧЕНЬ слабый квадрат = граница фронтальной грани, виден только в зонах мягких теней; диагноз: ложные блокеры в blocker search на боковых (grazing) зонах — широкий searchRadius при высокой Softness читает глубины склона самой стены -> фейковая полутень -> грубый лод MSM на склоне -> самозатенение+зерно; фронтальная грань чиста (перпендикуляр) -> граница = квадрат. Фикс (GLSL hot-reload): slope-aware per-tap маржа в point blocker search — tapMargin = irlite_depthBias(length(off), zPersp, near, far), компара refDepth - bias - tapMargin > s; поверхность <=45° к лучу не самоблокируется при любом радиусе; пропущенные поиском реальные контакт-блокеры всё равно рисуются MSM (резче). Спот-поиск ту же слабость имеет — отложено до тиража.

РАУНД 4 (2026-07-02, детали): диагностика юзера — карты penumbra/lod (debug 2/3) физически осмысленны (поиск блокеров чист), debug 4 (бленд OFF) УСИЛИЛ линии => линии живут в face-local фетче моментов у границ граней; бленд = полумера. РЕШЕНИЕ: hardware seamless cube-сэмплинг — texture VIEW GL_TEXTURE_CUBE_MAP_ARRAY над той же immutable RGBA32F-стораджей (96=16x6 слоёв), getGlTextureId() отдаёт view, компьюты пишут/читают 2D-array как раньше; миксины ОБОИХ модов: биндинг irl_pointEvsm 2D_ARRAY -> CUBE_MAP_ARRAY (лочстеп, addon+redactor правлены); GLSL: samplerCubeArray + textureLod(vec4(dir,slot),lod) — uvCE/halfTE/кламп/бленд/irlite_cubeFaceDir УДАЛЕНЫ (бесшовность у железа, GL_TEXTURE_CUBE_MAP_SEAMLESS on). Дебаг-режимы IRLITE_POINT_DEBUG сокращены до [0 1 2 3] (4=blend off устарел). Джар-маркеры view/msm верифицированы. Если линии выживут и это — остаётся только сам separable-блюр данных (переписать одним 2D-проходом в направлениях).

ИТОГИ РАУНДА 2 (2026-07-02, ин-ворлд юзер): (1) ПЕРЕКРЕСТЬЕ MSM PASS — Hamburger убил overlap-bleeding, bonus-баг закрыт; (2) полосы ±45 ЖИВЫ при выключенной пирамиде => пирамида НЕ источник, корень ниже по пути; (3) MSM peter-panning контакта управлялся слайдером IRLITE_SHADOW_BIAS (0.05 = отрыв, 0 = чисто). Пункты 2+3 сошлись на E4 root-cause дока: bias от refDist (евклид) при кодировке глубины от zPersp -> недобиас до 3x на боковых гранях с границей ±45. ФИКС ВКАЧЕН (GLSL, раунд 3, in-world pending): E4 = irlite_depthBias(... zPersp ...) в point-surface И VL-путях (1 идентификатор); + контакт отвязан от слайдера: под PREFILTER blockerCount==0 НЕ выходит в 1.0 — blockerDist=refDist -> penumbra=texel-floor -> MSM решает контакт по моментам; страховка if(blockerCount==0) return 1.0 после MSM-блока для инвалид-гейта. Пирамида ВСЁ ЕЩЁ ВЫКЛ (вернуть после подтверждения смерти полос).

ТИРАЖ — НАЗНАЧЕН НА СЛЕДУЮЩУЮ СЕССИЮ (команда юзера 2026-07-03). Содержимое: весь финальный point-стек из «ЗАКРЫТИЕ ТОЧКИ» (Ф0+Ф1+MSM4+cube-view+root-фиксы) на 6 инжектов (BSL/CR/RethinkingVoxels/Solas/Bliss/IterationRP) + регенерация .irlights патчей (команда дана). Эталон = закоммиченный run/shaderpacks/photon_v1.3b_IRLights/.../irlite_lights.glsl (addon e4935a0). Per-pack дельты: BSL уже имеет VL-stride но старой семантики s%N; IterationRP отстаёт сильнее всех (нет даже texel-floors/gather, см. shader-shadow-sampling PHOTON DELTA). Гочи: (1) samplerCubeArray textureLod + textureGather = GLSL 4.00, texelFetch/findMSB на #120-паках (BSL/Bliss) — прототипировать ПЕРВОЙ; (2) BSL = CRLF; (3) дебаг-дефайн IRLITE_POINT_DEBUG тиражировать (полезный инструмент) или вырезать — решить по месту; (4) Java-сторона тиража НЕ нужна (irl-core общий), только GLSL+патчи; (5) свежий core подхватывать по канону из feedback-addon-runclient-command (publishToMavenLocal + удаление loom-ремапа, НЕ --refresh-dependencies). Кандидат в тот же заход: спот-канал на MSM/root-фиксы (E4-аналог у спота консистентен, но slope-margins/contact-weighting у спота нет). Гоча запуска: runClient после правок irl-core требует publishToMavenLocal + --refresh-dependencies (stale-jar: аддон fd1b262 ссылается на IrlPatch.dof); gradle-демон 1G падает на remap sources под refresh — запускать с -Dorg.gradle.jvmargs=-Xmx3G.

КОЗЫРЬ ПРОЕКТА (консенсус всех отчётов): карты бейкаются и кэшируются -> бейк-препроцесс (blur/mips/пирамиды) амортизирован в ноль. Ни один шейдерпак так не может (их sun-map перерисовывается каждый кадр) — префильтруемые техники у нас выгоднее, чем где-либо.

ТЕКУЩАЯ ЦЕНА (Q2 default): 10 blocker + 18 PCF тапов на (свет,пиксель); spot gather 4 сравнения/тап, point бинарный compare; ротация spatial-only; fast=false всегда в surface (LOD нет); early-out нет. Бенчмарк хост-паков: blocker 3-6 (Photon 3!), PCF 6-16, temporal-джиттер под TAA у всех VPS-паков, hardware PCF (BSL/CR/Bliss) = бесплатный x4/тап (у нас COMPARE_MODE NONE), early-out Photon по первым 4 тапам, шарпенинг суб-тексельной пенумбры (Photon/Kappa).

РЕКОМЕНДОВАННЫЙ ПУТЬ (консенсус судей; каждый пункт под своим #define/флагом, старый путь = fallback):
- Ф0 GLSL-гигиена (только irlite_lights.glsl, ноль Java): early-out по первым 4 тапам; fast=true для тусклых/дальних (attenuation-LOD); фикс двойного полного PCSS в BSL outline (irlite_outlineInk зовёт fast=false); backport VL-stride BSL->Photon; унификация края тайла (сейчас 3 политики: blocker continue / PCF lit / gather clamp — бандинг у границы конуса); gather/сглаживание для point (несогласован со spot); IGN вместо sin-hash (highp); шарпенинг узкой пенумбры (кламп к ~2 texel + linstep); бюджеты тапов к нативным (резать default почти вдвое).
- Ф1 пирамидный PCSS: на бейке min/max/avg mip-пирамида (аддитивная RG-текстура рядом с depth; семантика карт, SSBO-контракт, VL НЕ меняются) -> в кадре 1 fetch early-out вне полутени (отсекает 70-90% пар свет-пиксель), avgBlocker 1 fetch вместо 6-20 тапов (заодно стабилизирует шумную оценку penumbra), адаптивные квантованные тапы (3-4 ступени против дивергенции). Java: аллокация пирамиды, downsample после endPass, учёт в shadowBakeBudget (не усугубить спайк C10 ~320ms). Гочи: mip-clamp чтобы footprint не пересёк тайл 4x4; textureLod на #120-паках (BSL/Bliss) — непроверенная гипотеза, прототипировать ПЕРВОЙ.
- Замер после Ф1 (FrameProfiler + in-world): если «зернистый квадрат» на высокой Softness или потолок качества остаются -> Ф2.
- Ф2 EVSM/MSM-префильтр: конверсионный пасс depth->moments ПОСЛЕ бейка (кастер-шейдеры не трогаются — их 3 пути рисуют depth как сейчас) + Gaussian blur + mip-цепочка; сэмплинг = Чебышёв 1 trilinear fetch при любой Softness; contact-PCF ветка по сырой depth для честного контактного затвердевания (гибрид). Spot = EVSM4 (fp32/fp16 по пресету), point = MSM RGBA16-UNORM или EVSM fp16 (страховка от bleeding). Закрывает «зернистый квадрат» ПО ПОСТРОЕНИЮ при любой из двух конкурирующих гипотез (бейк/сэмплинг — пост-мортем исключил шейдер, но GLSL-аудит нашёл в сэмплинге статичный шум ротации как кандидата; пре-блюр глушит обе); лечит bobbing-acne (нет бинарного compare) и даль (trilinear mips = shadow-AA); VL = 1 мягкий fetch на шаг.

РИСКИ Ф2: light bleeding в Minecraft-интерьерах (мультислой пол/мебель/потолок = худший кейс Чебышёва; ранний in-world тест интерьера обязателен); память x2-4 (HIGH live+static уже ~1.25GiB D32F; нужна лестница форматов по пресетам); gutter/padding тайлов атласа = правка layout-контракта Java+GLSL зеркально (исторически хрупко); per-face блюр куба без швов; VL-контракт Photon меняется (depth->moments).

ФАКТЫ БЕЙКА (аудит Java): Softness НЕВИДИМА бейк-стороне (grep пуст, не в сигнатуре — смена слайдера не ре-бейкает, влияет только на GLSL-радиусы); формат D32F оба layout, color-attachment нет (glDrawBuffer NONE); mip нет (MAX_LEVEL=0 захардкожен); spot NEAREST vs point LINEAR (LINEAR по raw depth = интерполяция глубин, сам по себе кандидат в артефакты граней); двухслойный static/live ping-pong + copyStaticToLive = готовый каркас под blur-пасс (blur вместо копии); единые точки аллокации createAtlas/createArray.

TEMPORAL: все 7 хост-паков джиттерят под TAA (Complementary/Solas отключают без TAA через #ifdef), но канон проекта = spatial-only ротация (осознанный отказ, shader-shadow-sampling). Вводить только как opt-in #define default OFF с TAA-гейтом, после явного пересмотра инварианта.

ОТКЛОНЕНО: screen-space blur теней (агрегированный буфер = кросс-блед пенумбр разных цветных источников; расходится с VL; ни один из 8 паков так не делает); SS contact shadows (только deferred Photon/IterationRP = 2/7 паков, аддитивная стоимость 8-16 тапов, целевые артефакты не закрывает); SDF-гибрид (шов «аналитическая тень блоков vs map-тень актёров» в одном кадре, потеря cutout-детали заборов/листвы, объём = отдельный проект); SAT-VSM (только spot, усиливает несогласованность spot/point).

РЕВЬЮ POINT-СТЕКА 2026-07-03 (multi-agent workflow, 108 агентов / 3 волны / adversarial-верификация «опровергни»; чекпоинт core 6668f22 / addon e4935a0 / redactor 57a7dfd; отчёт scratchpad tasks/wwt8u5k97.output). ВЕРДИКТ: 0 блокеров, 0 мажоров пережило опровержение -> point-стек ГОТОВ к тиражу на 6 паков с оговорками-гигиеной. Подтверждено верным (память-vs-код): NDC-ремап CONVERT есть (PointShadowEvsm.java:87 ndcZ=z01*2-1), E4-биас = zPersp сквозь surface+VL (не euclid), MSM-солвер каноничен (bias vec4(0.5)@1e-4 держит Ганкель-матрицу PD, ~2.5e-5 floor — не баг), пирамидный plane-margin leak-safe (max(dot,0.2) кламп over-darkens), validity-гейт mm.z<0 на ОБЕИХ surface-ветках (soft 730 + fast/hard 590), mixin'ы addon<->redactor семантически идентичны. Рейзилось-и-опровергнуто: Hamburger NaN при c.z=0 (Dirac lz=0/zf=0) — moment-bias регуляризует, точный коллапс невозможен. ПОПРАВКИ ПАМЯТИ: (1) момент-биас = 1.0e-4 дефайн (bonus-bag «3e-7» СТЕЙЛ — это лишь левый пункт слайдера [3e-7...1e-3]); (2) «Plan B код удалён» НЕТОЧНО — ветка функц. мертва (MSM returns unconditionally @737), но каркас irlPreVisP/irlPreWP жив и vestigial (irlite_lights.glsl:713-714 init, 763 гард константно-true, 785 mix недостижим); (3) Ф2b «биндинг не менялся / rebound 2D_ARRAY» ОТМЕНЁН раундом 4: реальный биндинг = GL_TEXTURE_CUBE_MAP_ARRAY (texture view, getGlTextureId()->viewId) — но 2 стейл-комментария в коде ещё пишут «2D_ARRAY» (ProgramSamplersBuilderMixin.java:39 + редактор:42, PointShadowEvsm.java:19 джавадок) -> править до тиража; (4) «#endif without #if» ЗАКРЫТ для нашего стека: все IRLite-файлы balanced (irlite_lights.glsl 56 opens=56 closes depth 0; d4_deferred_shading.fsh + c0_vl.fsh depth 0) — источник шума вне IRLite (база Photon/Iris-сканер); (5) validity-гейт: формулировка «обе ветки» = surface да, VL нет по дизайну (VL читает irl_pointShadowArray raw-depth, irl_pointEvsm не трогает). НАХОДКИ (все minor/nit/info): (A, единств. корректностный) far-floor трёхсторонний рассинхрон — ShadowRenderer.java:173 = max(radius,NEAR+0.1)=0.15 бейк-проекция / PointShadowEvsm.java:193 = max(radius,0.1) MSM-декод / irlite_lights.glsl:534 = max(posRadius.w,0.001) GLSL-сравнение; тривигерится radius<0.15 (degenerate/непортящий свет), но кросс-каттинг (спот ShadowRenderer.java:131 та же NEAR+0.1 vs GLSL:318 0.001); GLSL-floor 0.001 размножится в 6 паков -> выровнять или задокументировать до тиража; (B) overlay-перф: point overlay re-prefilter ВСЕХ 6 граней MSM+пирамида/кадр без бюджета/face-mask (ShadowBaker.java:724 markDirty slot-only; = известный open Ф2a, Java-сторона общая); (C) slider-аннотации без screen.*/sliders= (irlite_lights.glsl:17-кластер: NORMAL_OFFSET/EVSM_BLEED/EVSM_BLEED_POINT/MSM_MOMENT_BIAS/VL_SHADOW_STRIDE) + IRLITE_POINT_DEBUG:38 «// [0 1 2 3]» — решить expose/strip до размножения; (D) мёртвый каркас Plan B (см. поправка 2) — вычистить в эталоне до тиража. ГЕЙТ ТИРАЖА ПРОЙДЕН; рекоменд. закрыть A/C/D в эталоне (irlite_lights.glsl) ДО размножения в 6 копий, B — один Java-фикс на всех. Код-фиксы ждут решения юзера по отчёту.

Связь: [[shadow-distance-quality-plan]] (KNOWN-OPEN зернистый квадрат + texel-floors done), [[project-shadow-bake-perf-audit]] (C10 спайк), [[addon-shadows]] (бейк-движок), [[shader-shadow-sampling]] (контракт чтения, инвариант spatial-only).
