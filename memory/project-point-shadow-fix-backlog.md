---
name: project-point-shadow-fix-backlog
description: "Бэклог фиксов после код-ревью point-стека 2026-07-03 (5 пунктов А-Д, все minor/nit + Java-перф + спот-парити); гейт тиража пройден, фиксы опциональны и не начаты; для отдельной сессии."
metadata: 
  node_type: memory
  type: project
  originSessionId: ec96bee9-a1c0-4a7b-92c1-cde09d2b480f
---

Бэклог фиксов из multi-agent код-ревью point-канала теней (108 агентов, 2026-07-03). Гейт тиража на 6 паков ПРОЙДЕН и БЕЗ этих фиксов (0 блокеров, 0 мажоров). Всё ниже = гигиена/косметика/перф + один degenerate-корректностный. СТАТУС 2026-07-03: пункт А ВЫПОЛНЕН в эталоне (Photon); Б/В/Г/Д не начаты. Полный отчёт: scratchpad tasks/wwt8u5k97.output. Контекст, вердикт и поправки памяти: [[plan-shadow-filtering-refactor]] блок РЕВЬЮ POINT-СТЕКА. Чекпоинт-коммиты: core 6668f22 / addon e4935a0 / redactor 57a7dfd. Эталон GLSL = addon run/shaderpacks/photon_v1.3b_IRLights/shaders/include/irlite/irlite_lights.glsl.

ВАЖНО перед стартом: код-ревью был read-only на этих коммитах — перед правкой пере-верифицировать строки (могли сдвинуться). Пункты независимы, порядок А->Г любой; рекоменд. закрыть А,В,Г в ЭТАЛОНЕ до тиража (чтобы не размножать мусор в 6 копий), Б опц., Д — только если спот идёт в тот же заход.

## HANDOFF СЕССИИ 2026-07-03 (Photon merge DONE -> Phase 2 тираж) — ЧИТАТЬ ПЕРВЫМ
МИНИ-ПРОМПТ новой сессии: «Продолжаем тираж shadow-фильтрации на 6 паков (Phase 2). Photon ГОТОВ. Читай этот HANDOFF. ПЕРВОЕ: закоммить UNCOMMITTED re-exposure NORMAL_OFFSET. Потом продолжай тираж с CR.»

## ПРОГРЕСС ТИРАЖА (обновл. 2026-07-03, сессия-продолжение)
- NORMAL_OFFSET re-exposure ЗАКОММИЧЕН: e00cbf3 (patches/photon.irlights + run/photon_v1.3b glsl).
- КАДЕНС КОММИТОВ (решение юзера): ПО ПАКУ (как Photon), каждый пак = свой коммит. Коммичу после юзер-PASS + байт-валидации патча.
- CR (ComplementaryReimagined) — DONE + COMMITTED 69ecbda (in-game PASS юзером 2026-07-04, патч байт-идентичен Modification). Метод сплайса: Photon эталон glsl = золотой источник shadow-секции; Python-скрипт splice_cr_shadow.py (в scratchpad) заменил CR-секцию [строки 139..400] на Photon [161..779] (сэмплеры→функции→#endif, самодостаточный блок) + вставил define-блок Photon [18..37] после NORMAL_OFFSET. Файл: Shadres/Modification/ComplementaryReimagined/shaders/lib/irlite/irlite_lights.glsl (957->1344 стр, LF; Photon источник CRLF -> конвертнут). Бэкап pre-port: scratchpad CR_irlite_lights.glsl.bak-preport. Реген: tools/gen-complementary-patch.ps1 (читает Modification вербатим, ассерты анкеров); валидация: java -cp tools/build PatchHarness (-validate + apply + git diff --no-index vs Modification = пусто).
- CR-адаптации под #version 130 (это и есть «адаптируя под #version/сэмплеры»): CR УЖЕ имел #extension cube_map_array+texture_gather; ДОБАВЛЕНЫ 2: GL_EXT_texture_array (sampler2DArray irl_pointShadowPyramid) + GL_ARB_gpu_shader5 (findMSB + textureGather(cubeArray,comp)). Все 4 extension в shadow-блоке (строки ~156-159). Photon источник их НЕ несёт (у Photon выше #version, фичи core).
- RV (RethinkingVoxels, CR-форк) — ПОРТ КОДА DONE, патч регенерён+провалидирован (20 ops, байт-идентичен Modification), ин-гейм тест PENDING (юзер). RV shadow-секция БЫЛА байт-идентична CR pre-port (diff 130-401 пусто; отличие только стр.37 комментарий OUTLINE) -> применён generic-скрипт splice_shadow.py <target> (те же границы, тот же Photon [161..779]+defines[18..37]) + те же 2 правки (LOD-caller smoothnessG<0.7, extension-коммент). Результат: RV shadow-секция (152-780) БАЙТ-ИДЕНТИЧНА CR. Реген: gen-rethinkingvoxels-patch.ps1. Тест-пак: run/shaderpacks/RethinkingVoxels_IRLights. Форк-дельты RV (COMPOSITE не COMPOSITE1, VL=colortex15) — ВНЕ shadow-секции, не затронуты. splice_shadow.py = ШАБЛОН для остальных CR-lineage паков (проверить границы assert'ами перед прогоном).
- BSL (#version 120 forward) — ПОРТ КОДА DONE, патч регенерён+провалидирован (29 ops, байт-идентичен Modification), ин-гейм тест PENDING (юзер) — САМЫЙ рискованный по компиляции (#120). Инструмент: splice_shadow2.py (CONTENT-BASED, ищет границы по содержимому — надёжно для сдвинутых паков; источник = git HEAD эталон извлечён в scratchpad/photon_etalon_HEAD.glsl). BSL границы сдвинуты (NORMAL_OFFSET@18, ext@135-136, atlas@139, #endif@399) — content-based скрипт справился, shadow-body вышел БАЙТ-ИДЕНТИЧЕН CR. BSL факты #120: уже юзает texture()+textureSize() без явного GL_EXT_gpu_shader4 -> NVIDIA даёт #130-семантику; добавил те же 2 ext (texture_array+gpu_shader5). РИСК если не скомпилится: texelFetch/textureLod/findMSB при #120 -> добавить GL_EXT_gpu_shader4 (texelFetch) и/или GL_ARB_shader_texture_lod (textureLod), смотреть Iris-лог. BSL-специфика caller: НЕТ gloss-переменной в scope (specular в irlite_specGGX) -> LOD-гард БЕЗ roughness-исключения (dim->fast всегда); caller передаёт `hasNormal ? n : L` не `n`. BSL оказался LF (память «CRLF» устарела). Тест-пак: run/shaderpacks/BSL_IRLights.
- Solas (#130 forward, ru_RU) — ПОРТ КОДА DONE, патч регенерён+провалидирован (19 ops, байт-идентичен Modification), ин-гейм тест PENDING. Solas shadow-секция была БОЛЬШЕ (139-423 vs CR 140-400) но差ница = только более многословные комменты, функции те же -> content-based сплайс заменил всё тело, вышло БАЙТ-ИДЕНТИЧНО CR. Caller как BSL (`hasNormal ? n : L`, LOD без gloss-гарда). Тест-пак: run/shaderpacks/Solas_IRLights. gen-solas-patch.ps1.
- Bliss (#120 deferred+forward) — DONE + COMMITTED 283256b (in-game PASS). Content-based сплайс, body байт-идентичен CR, caller как BSL.
- СТАТУС ТИРАЖА (2026-07-04): 5/6 COMMITTED — CR 69ecbda, RV f3b3f37, BSL 697373c, Solas 19e6b4e, Bliss 283256b. IterationRP (6/6) — КОД+ПАТЧ DONE 2026-07-04 (отдельная сессия), ин-гейм PENDING (юзер), КОММИТ ЗАБЛОКИРОВАН политикой .gitignore (см. ниже). Полный recon+план ниже в блоке «IterationRP HANDOFF».
- IterationRP ПОРТ ВЫПОЛНЕН 2026-07-04: splice по плану (defBlock Photon[18..37] после NORMAL_OFFSET@20; body Photon[161..779] заменил IterationRP[125..387] дропнув свой sampler-comment@125; БЕЗ extension — нативный #430; caller@512 LOD-гард `&& roughness > 0.3` напрямую, irlShFast в оба вызова). Скрипт splice_iterationrp.py (scratchpad, с ассертами границ). Бэкап pre-port: scratchpad IterationRP_irlite_lights.glsl.bak-preport. ВАЛИДАЦИЯ (validate_iterationrp.py, 22/22 PASS): body==CR-body БАЙТ-ИДЕНТИЧЕН (32609B, golden один), def-block==CR, препроц 57/57, скобки 75/75, no-arg rotationPhi=0, 4 сэмплера+msmHamburger/pointGatherTap/chebyshev/cubeFaceUV есть, 0 добавленных extension, VL unshadowed (irlite_vlSpotStep/vlPointStep@1041-1090) не тронут. Файл 896->1280 стр. Патч regenerated (gen-iterationrp-patch.ps1, 1508 стр, 12 ops), PatchHarness -validate + apply = ok=true, git diff --no-index applied/shaders vs Modification/shaders = ПУСТ. Тест-пак run/shaderpacks/IterationRP_IRLights создан (cp Modification, 1280 стр glsl).
- !!! КОММИТ-БЛОКЕР IterationRP (найдено 2026-07-04): patches/iterationrp.irlights И tools/gen-iterationrp-patch.ps1 НАМЕРЕННО в .gitignore:133-135 с коммент «IterationRP integration — private/paid shader, hidden from repo pending author permission (kept local only)». В отличие от 5 др. паков (патчи трекаются), IterationRP держится ЛОКАЛЬНО. Обычный `git add` = no-op; `git add -f` продавит лицензионный холд. РЕШЕНИЕ ЮЗЕРА 2026-07-04 = вариант (а): ОСТАВИТЬ ЛОКАЛЬНО как задумано политикой — КОММИТА НЕТ, .gitignore не трогать, `-f` не форсить. Тираж КОДА 6/6 завершён (патч живёт на диске, воспроизводим из Modification через gen-iterationrp-patch.ps1). НЕ пере-предлагать коммит IterationRP main без прямой команды. Флоу gen+PatchHarness штатно локальный, git не трогает.

## КОМБО DoF IterationRP — ВЫПОЛНЕНО + ЗАКОММИЧЕНО 2026-07-04
Коуплинг: IRL-инжект живёт в ДВУХ патчах — bbs-irlights-addon/patches/iterationrp.irlights (световой, gitignored) И bbs-dof-addon/patches/iterationrp-irl-dof.irlights (combo IRL+DoF, применяется ВМЕСТО светового). После порта shadow-стека в главный — combo был STALE (несёл старое 896-стр тело). По прямой команде юзера «перенести это в комбо доф»: combo `+file shaders/Lib/irlite_lights.glsl`-тело (стр.15-910 между <<<@14 и >>>@911) заменено содержимым Modification/IterationRP (896->1280 стр) скриптом combo_replace_body.py (scratchpad, бэкап iterationrp-irl-dof.irlights.bak-preport). ВЕРИФИКАЦИЯ: (1) до замены combo-тело БАЙТ-ИДЕНТИЧНО pre-splice Modification (896 стр/36147B — DoF-дрейфа внутри glsl НЕТ, DoF-слой только в отд. файлах); (2) whole-tree диф applied-new vs applied-old combo = изменён РОВНО 1 файл irlite_lights.glsl (+429/-45), DoF-слой (irl_dof_focus.glsl + DOF.glsl 21 хук + все hook-ops) байт-идентичен; (3) applied irlite_lights.glsl == Modification байт-в-байт; (4) PatchHarness apply = 25 ops ok (12 IRL + 13 DoF). ГОЧА ТУЛИНГА: bbs-irlights-addon/tools/build/PatchHarness (пакет qualet.irlite.client.patcher) НЕ знает директиву `@dof` -> ParseException. @dof-aware парсер = irl-core `org.qualet.irl.patcher` (IrlPatchParser:117). Пересобрал harness: `javac -d <build> scratchpad/PatchHarness.java(imports org.qualet.irl.patcher.*) + irl-core .../{IrlPatch,IrlPatchParser,PatchEngine,IrlPatchApplier,PatchResult}.java` (5 core чистые java.*, API идентичен старому). КОММИТ: юзер решил (bbs-dof-addon = приватный репо -> коммитить можно, visibility-нюанс moot) -> bbs-dof-addon master 736fa7d, `git add patches/iterationrp-irl-dof.irlights` (не -A), не запушено. Combo хранится LF (i/lf, autocrlf clean-фильтр, как все combo). ИН-ГЕЙМ combo не тестили (shadow-glsl идентичен главному порту, DoF-слой не тронут -> поведение теней идентично). Тираж shadow-фильтрации ПОЛНОСТЬЮ ЗАКРЫТ: 6/6 main + IterationRP combo.

## РЕЮЗ-ФЛОУ per-pack (отработан на 5 паках, шаблон для IterationRP)
1. Золотой источник shadow-секции = Photon эталон из git HEAD: `git show HEAD:run/shaderpacks/photon_v1.3b_IRLights/shaders/include/irlite/irlite_lights.glsl > <scratch>/photon_etalon_HEAD.glsl` (1290 стр; landmarks: NORMAL_OFFSET@17, `// ---- baked shadow samplers`@161, `#endif // IRLITE_COMPILE_SHADOWS`@779). НЕ юзать рабочий run/-файл (юзер перенёс его в run/shaderpacks/1/; трекнутый путь показывает D в git — НЕ коммитить эту D).
2. Сплайс: defBlock = Photon[18..37] (blank + F0/F1/F2 дефайны) вставить после строки NORMAL_OFFSET пака; shadow-body = Photon[161..779] (comment+сэмплеры+функции+#endif) заменяет [сэмплер irl_spotShadowAtlas .. #endif // IRLITE_COMPILE_SHADOWS] пака. Для CR-lineage (#120/#130) ещё +2 #extension (GL_EXT_texture_array + GL_ARB_gpu_shader5) после последнего существующего #extension в shadow-блоке. Скрипт splice_shadow2.py (content-based, был в scratchpad — пересоздать) ищет границы по содержимому; результат body ДОЛЖЕН выйти байт-идентичным CR (проверять awk-срезом от `// ---- baked shadow samplers` до `#endif // IRLITE_COMPILE_SHADOWS` vs CR committed).
3. Caller LOD-правка (в irlite_lightSurface, блок «ONE shadow visibility shared by diffuse + specular»): обернуть в `#ifdef IRLITE_SHADOW_LOD ... bool irlShFast = attenuation*light.colorIntensity.a < IRLITE_SHADOW_LOD_THRESHOLD [&& <gloss>]; #else bool irlShFast=false; #endif` и передать irlShFast вместо false в оба вызова. gloss-выражение per-pack: CR/RV `&& smoothnessG < 0.7`; BSL/Solas/Bliss — БЕЗ gloss (нет переменной, specular в GGX-хелпере); нормаль-арг сохранять как у пака (CR `n`; BSL/Solas/Bliss `hasNormal ? n : L`). rotationPhi: Photon-версия `(int seq)` приходит с body, вызовов no-arg остаться 0.
4. Валидация: python баланс (#if/#endif равны, скобки равны, rotphi no-arg=0, 4 новых сэмплера/msmHamburger/pointGatherTap есть). Тест-пак: cp -r Shadres/Modification/<Pack> run/shaderpacks/<Pack>_IRLights (юзер переносит в run/shaderpacks/1/ и тестит hot-reload). Реген: tools/gen-<pack>-patch.ps1. Патч-валидация: `java -cp tools/build PatchHarness -validate <patch> Shadres/Original/<Pack>` + apply в scratch + `git -c core.autocrlf=false diff --no-index --ignore-cr-at-eol <applied>/shaders Shadres/Modification/<Pack>/shaders` = ПУСТО.
5. Коммит ТОЛЬКО после юзер-PASS (каденс по-паку): `git add patches/<pack>.irlights` (явно, НЕ -A — иначе схватит D photon_v1.3b), msg «<Pack>: port full shadow-filtering stack (F1 pyramid + F2 EVSM/MSM + adaptive + gather)» + Co-Authored-By.

## IterationRP HANDOFF (recon 2026-07-04, ПОРТ НЕ НАЧАТ)
Структурно БЛИЖЕ к CR чем боялись, но 3 отличия. Файл: Shadres/Modification/IterationRP/shaders/Lib/irlite_lights.glsl (ЗАГЛАВНАЯ Lib, БЕЗ irlite/ подпапки; 896 стр, LF). #version 430 нативный (deferred).
- ОТЛИЧИЕ 1 (extensions): shadow-блок IterationRP НЕ содержит #extension (123-127 = #ifdef -> blank -> comment -> сэмплеры сразу; native #430 = samplerCubeArray/sampler2DArray/findMSB/texelFetch/textureLod/textureGather всё core). splice_shadow2.py упадёт на i_lastext («NO #extension in target shadow block») — это ОЖИДАЕМО. Для IterationRP: НЕ добавлять 2 extension; границу брать по строке сэмплера irl_spotShadowAtlas (не по последнему #extension). (GL_EXT_texture_array есть на стр.83 для cookie — не трогать.)
- ОТЛИЧИЕ 2 (path): патч-генератор gen-iterationrp-patch.ps1 + путь shaders/Lib/irlite_lights.glsl. Проверить что генератор читает верный путь.
- ОТЛИЧИЕ 3 (caller — ЛУЧШЕ остальных): сигнатура `void irlite_lightSurface(vec3 worldPos, vec3 normal, vec3 viewDirW, float roughness, vec3 f0, ...)` @444 — ЕСТЬ `float roughness` в scope. Caller @512-515 «ONE shadow visibility» передаёт `n` (как CR, не hasNormal?n:L). LOD-гард -> `&& roughness > 0.3` НАПРЯМУЮ (точный паритет Photon `material.roughness>0.3`, лучше чем smoothnessG-адаптация).
- Границы IterationRP: NORMAL_OFFSET@20, IRLITE_COMPILE_SHADOWS #ifdef@123, сэмплеры irl_spotShadowAtlas@126/irl_pointShadowArray@127, #endif // IRLITE_COMPILE_SHADOWS@387. IRLITE_SURFACE_PASS gate есть (@390) — как CR. Сигнатуры irlite_spotShadow/pointShadow @182/@300 = `(vec3 fragWorld, vec3 normal, IrliteLight light, bool fast)` идентичны Photon. rotationPhi()@160 no-arg -> мигрируется body-заменой.
- ПЛАН IterationRP: (a) defBlock Photon[18..37] после стр.20; (b) заменить [стр.126 сэмплер .. стр.387 #endif] на Photon[161..779] (или [162..779] если оставлять свой sampler-comment@125), БЕЗ добавления extension; (c) caller @512-515: обернуть LOD с `&& roughness > 0.3`, передать irlShFast (нормаль `n`); (d) валидация балансов + body==CR-body проверка (должен совпасть — golden один); (e) тест-пак run/shaderpacks/IterationRP_IRLights (юзер в /1/); (f) gen-iterationrp-patch.ps1 + PatchHarness байт-валидация; (g) юзер-PASS -> commit. Риск компиляции у #430 = минимальный (всё core). ВНИМАНИЕ: IterationRP VL unshadowed ([[shader-iterationrp-pipeline]]) — VL-путь не сэмплит тени, проверить что body-замена не тронула ничего вне shadow-блока.
- Java-сторона НЕ трогается per-pack: ProgramSamplersBuilderMixin биндит все 7 сэмплеров ГЛОБАЛЬНО по имени (build() HEAD, addDynamicSampler=no-op если uniform не объявлен); SamplerBindingCubeArrayMixin ремапит GL-таргеты по id (cube-array для pointArray/pointEvsm, 2D_array для pointPyramid/cookie) — тоже глобально. CR получает биндинги автоматически, объявив сэмплеры в GLSL. Это снимает главный риск тиража на ВСЕ 6 паков.
- Caller LOD: Photon surface-caller считает irlShFast из IRLITE_SHADOW_LOD с `material.roughness>0.3`; у CR нет Material-структуры -> адаптирован на `smoothnessG < 0.7`. Outline-caller CR оставлен `false` (нет material в scope). VL-путь (vlSpotStep/vlPointStep) НЕ трогался — сэмплит raw irl_spotShadowAtlas/irl_pointShadowArray напрямую.
- Валидация статическая: препроцессор 62/62, скобки 76/76, rotationPhi мигрирована в (int seq) без dangling no-arg вызовов, 4 сэмплера + chebyshev/msmHamburger/pointGatherTap/cubeFaceUV на месте.
- Тест-пак: run/shaderpacks/ComplementaryReimagined_IRLights (копия Modification/CR, параллельно Photon_IRLights). Юзер: выбрать пак в Iris GUI -> hot-reload (дев-клиент javaw уже запущен, второй runClient НЕ пускать — конфликт). Активный пак пока Photon_IRLights (iris.properties НЕ трогал).
- ПОСЛЕ юзер-PASS: реген патча patches/complementaryreimagined.irlights (диф Modification/CR vs Shadres/Original/CR). НЕ регенить до PASS.
- ОСТАЛОСЬ по паку (шаблон для 5 следующих): RethinkingVoxels (CR-форк, ближайший), BSL #120 CRLF, Solas #130, Bliss #120, IterationRP #430 (нативно, extension НЕ нужны).

СДЕЛАНО (Photon полностью сведён и закоммичен):
- пункт А (GLSL-гигиена) + POINT_DEBUG ВЫРЕЗАН в Photon.
- VL_NOISE влит в Photon MSM-glsl (5 деф + irlite_noise3D/irlite_vlNoise + 3 march-хука; препроц 57/57, скобки 75/75, in-game PASS юзером).
- VL_SHADOW_STRIDE выведен в Photon настройки «Shadow Tap Stride»/«Шаг теневых тапов» (Photon отставал; CR/BSL референс).
- NORMAL_OFFSET: РЕШЕНИЕ ЮЗЕРА = ВЫВЕДЕННЫМ ВЕЗДЕ. Re-exposed в Photon (patch photon.irlights + photon_v1.3b glsl + Photon_IRLights: screen/sliders/en_US/ru_RU + glsl-аннотацию `// [0.0 0.02 0.05 0.10 0.15 0.25 0.40]`). 6 паков уже выводят -> НЕ трогать.
- Photon-патч regenerated + COMMITTED 30040cf (glsl-блок byte-verified, heredoc 20/20).
!!! UNCOMMITTED в рабочем дереве: re-exposure NORMAL_OFFSET (patches/photon.irlights + run/.../photon_v1.3b_IRLights/...glsl). ЗАКОММИТИТЬ первым в новой сессии. Msg: "Photon: re-expose IRLITE_SHADOW_NORMAL_OFFSET (parity with other packs)". git add -f для run/ пути (gitignore).

ТОПОЛОГИЯ Photon-паков: run/shaderpacks/Photon_IRLights = АКТИВНЫЙ дев-пак (iris.properties shaderPack=Photon_IRLights), полный, UNTRACKED. photon_v1.3b_IRLights = TRACKED эталон glsl, тоже полный. Оба glsl байт-идентичны. Бэкап старого Photon_IRLights (891 стр pre-merge) в scratchpad.

КОРРЕКЦИЯ СКОУПА ТИРАЖА (критично, найдено в конце сессии): тираж = НЕ «добавить MSM», а перенос ВСЕГО стека фильтрации теней. CR (и все 6 по счётчикам) имеют только базовый PCSS (BLOCKER_TAPS+PCF_TAPS), но НЕ: SHADOW_PREFILTER(F2 EVSM), SHADOW_PYRAMID(F1 min/max), SHADOW_ADAPTIVE, POINT_GATHER, MSM(Hamburger).
Photon shadow-сэмплеры (patches/photon.irlights): 171 irl_spotShadowAtlas, 172 irl_pointShadowArray (эти 2 есть у CR) + НОВЫЕ для тиража: 174 irl_spotShadowPyramid, 175 irl_pointShadowPyramid (F1 RG32F min/max), 178 irl_spotEvsm (F2 EVSM4 spot), 179 irl_pointEvsm (MSM4 point cube). Java-бейк (irl-core Spot/PointShadowPyramid+Evsm) ОБЩИЙ -> данные пекутся для всех паков, надо чтоб GLSL пака их ОБЪЯВИЛ+сэмплил (+ mixin-биндинги проверить per-pack).
Photon shadow-секция patches/photon.irlights: дефайны F1/F2/MSM ~27-46, сэмплеры 171-179, msmHamburger@192, spot@324, point@541, cookie@798 (~470 стр секция). CR patches/complementaryreimagined.irlights: cookie@103, сэмплеры 149-150, spot@205, point@322 (базовый PCSS, меньше).

ПОДХОД (предложен, НЕ начат): пересадить Photon shadow-секцию (F1/F2/MSM дефайны + 4 новых сэмплера + msmHamburger + spot/point функции + pyramid/gather/adaptive хелперы) в каждый пак, заменив базовую PCSS-секцию. Адаптировать per-pack: объявления сэмплеров (CR #version 130 -> #extension для samplerCubeArray/textureGather, см. CR:144; IterationRP #430 нативно; BSL #120 CRLF; Solas #130; Bliss #120; RethinkingVoxels CR-форк). Дефайны F1/F2/MSM ВНУТРЕННИЕ (не в настройки). По одному паку + in-game тест каждого. Старт: CR (юзер выбрал; forward #130, референс). Порядок остальных 5 — по подобию.
Per-pack пайплайн-заметки: [[complementary-pipeline]] [[rethinkingvoxels-pipeline]] [[bsl-pipeline]] [[solas-pipeline]] [[bliss-pipeline]] [[shader-iterationrp-pipeline]]. Контракт сэмплинга: [[shader-shadow-sampling]]. Общий glsl-контракт: [[shader-irlite-glsl]].

## А — GLSL-гигиена в эталоне (zero-risk cleanup, косметика) — ВЫПОЛНЕНО 2026-07-03
Severity nit/minor. Риск ~ноль (мёртвый/невыполняемый код и метадата). Делать ДО тиража — иначе размножится в 6 паков.
ВЫПОЛНЕНО 2026-07-03 в эталоне run/shaderpacks/photon_v1.3b_IRLights/.../irlite_lights.glsl (git 7+/16-, net -9 стр): А1 удалён мёртвый Plan B каркас (обе декл irlPreVisP/irlPreWP :713-714, гард irlPreVisP<0 сведён к #else :763, недостижимый mix-блок :784-786) — грепом 0 остаточных ссылок, поведение байт-идентично (irlPreVisP всегда -1.0). А2 IRLITE_POINT_DEBUG ОСТАВЛЕН как compile-time dev-tool (вариант a), снят обманный `// [0 1 2 3]` -> описательный коммент "not on the settings screen". А3 снят инертный `// [...]` с 5 дефайнов (NORMAL_OFFSET, EVSM_BLEED, EVSM_BLEED_POINT, MSM_MOMENT_BIAS, VL_SHADOW_STRIDE) -> описательные комменты (вариант СНЯТЬ); проверено грепом что все 5 отсутствуют в shaders.properties screen.*/sliders= и en_US.lang. Прочие 6 паков НЕ трогались (получат чистый эталон при тираже).
ОТЛОЖЕННЫЕ РЕШЕНИЯ (обратимы, только по прямой команде): А2-alt = вырезать IRLITE_POINT_DEBUG целиком (seam-диаг закрыта); А3-alt = ЭКСПОРТ IRLITE_SHADOW_NORMAL_OFFSET (единственный из 5 стоит в 'exposed'-блоке под шапкой "options (exposed...)" но не в screen.* — намёк на недовыведенную опцию) как юзер-слайдер рядом с SHADOW_BIAS: потребует screen.irlite_shadows + en_US.lang option.* + тираж на 6 паков (scope-расширение, НЕ гигиена). Оставлен на месте с честным комментом "not currently on the settings screen".
РЕШЕНО 2026-07-03 (прямая команда юзера, оба пункта): А2 -> IRLITE_POINT_DEBUG ВЫРЕЗАН ЦЕЛИКОМ (define + блоки #if==1/==2 + #if==3) в эталоне И синкнуто в Shadres/Modification/Photon; оба .glsl = 1239 стр, байт-идентичны, грепом POINT_DEBUG=0. А3 -> IRLITE_SHADOW_NORMAL_OFFSET ВРЕМЕННО ВЫВЕДЕН в Iris-экран ТОЛЬКО в run/ эталоне для ин-гейм проверки: вернул слайдер-аннотацию `// [0.0 0.02 0.05 0.10 0.15 0.25 0.40]` на дефайн + добавил в shaders.properties (screen.irlite_shadows + sliders=) + en_US.lang option/comment с §c[TEMP]§r. Дефайн РЕАЛЬНО используется (spot :333-334, point :543-550, VL :1054/:1216) -> слайдер даёт видимый эффект: мало=контакт-тень но acne, много=peter-panning без acne. ВАЖНО: в паке Photon shaders.properties+lang GITIGNORED (tracked только .glsl) -> экспозиция NORMAL_OFFSET живёт на диске для теста, В ГИТ НЕ ПОПАДЁТ; run/ .glsl разошёлся с Shadres/Mod ровно на 1 строку (:17 temp-аннотация vs "not on screen"). ОТКРЫТО: финальное решение по NORMAL_OFFSET (оставить/убрать/промоутнуть в Shadres+патч+тираж) ПОСЛЕ визуальной проверки юзером.
- А1 мёртвый каркас Plan B: irlite_lights.glsl:713-714 (`float irlPreVisP=-1.0; float irlPreWP=0.0;`), :763 (гард `irlPreVisP<0.0` константно-true), :785 (`if(irlPreVisP>=0.0) mix(...)` недостижим). Fix: удалить обе переменные; строку 763 свести к #else-варианту (764-766 = `if(i==4 && (sum<=1e-3||sum>=4.0-1e-3)) return sum*0.25;`); удалить блок 784-786. Проверка: point MSM всегда `return vis` @737, блендозоны у точки нет по дизайну (в отличие от спота, где irlPreVis ЖИВ 445/475/477/501/519 — спот НЕ трогать).
- А2 IRLITE_POINT_DEBUG: irlite_lights.glsl:38 `#define ...0 // [0 1 2 3]` + ветки :703-707, :725-727. Аннотация `// [...]` обманная (не на screen.*). Решить: (a) оставить как dev-tool, но снять `// [0 1 2 3]` (как у IRLITE_SHADOW_IGN:20 без списка), либо (b) вырезать целиком если seam-диагностика закрыта. Плановая гоча тиража #3.
- А3 slider-аннотации без экрана: irlite_lights.glsl:17-кластер несёт `// [...]` но их нет в shaders.properties screen.*/sliders= и в en_us.lang option.*. Defines: IRLITE_SHADOW_NORMAL_OFFSET(17), IRLITE_EVSM_BLEED(34), IRLITE_EVSM_BLEED_POINT(35), IRLITE_MSM_MOMENT_BIAS(37), IRLITE_VL_SHADOW_STRIDE(43). Решить: либо ЭКСПОРТ (добавить в screen.irlite_shadows/volumetric + sliders= + lang option.*) — если крутилки нужны юзеру, либо СНЯТЬ `// [...]` — если это внутренние. Определить до тиража (иначе 6× мёртвой метадаты). Iris не авто-аппендит нелистнутые опции.

## ДРЕЙФ Shadres/Modification/Photon vs эталон — РЕШЁН 2026-07-03 юзером (был тираж-хазард)
РЕШЕНО: юзер синкнул Shadres/Modification/Photon <- эталон (полная перезапись файла: до моих правок оба были байт-идентичны 1249 стр). Теперь оба = 1239 стр (вкл. пункт А + вырезанный POINT_DEBUG), тираж-хазард снят. УТОЧНЕНИЕ по VL_NOISE (ранее было сформулировано алармистски — исправлено): VL_NOISE НЕ потерян и в этой сессии НЕ редактировался/не удалялся. Полная фича (5 дефайнов + код irlite_noise3D/irlite_vlNoise + интеграция в ray-march + screen + sliders + lang, ~40 строк) ЖИВЁТ в patches/photon.irlights — закоммичена (6545101 «VL-noise port ... Photon», в текущем HEAD). Это durable-артефакт (working-copy Shadres = транзитная). Синк юзера перезаписал лишь дефайны VL_NOISE в транзитной Shadres/Mod/Photon; 100% реконструируется применением/диффом photon.irlights -> НУЛЕВАЯ потеря фичи. РЕАЛЬНАЯ (пред-существующая) картина Photon: run/ эталон = MSM-point-стек БЕЗ VL_NOISE; photon.irlights = VL_NOISE + settings-unified но БЕЗ нового MSM-стека (патч последний раз трогали 3b3d79a 2026-07-02, до MSM-рефактора 2026-07-03). При тираже/патч-регене Photon ОБЕ линии слить (обе фичи сохранны). Историческая справка ниже актуальна как контекст.

## ТОПОЛОГИЯ Photon-паков в run/ + слияние MSM+VL_NOISE (2026-07-03, сессия)
В run/shaderpacks ДВА Photon-IRLights пака (память «эталон=photon_v1.3b» УСТАРЕЛА):
- Photon_IRLights — АКТИВНЫЙ в дев-клиенте (run/config/iris.properties shaderPack=Photon_IRLights); имеет ребренд-настройки IRLIGHTS_* + VL_NOISE-настройки (из 3b3d79a). Был со СТАРЫМ glsl (891 стр, без MSM). ЦЕЛЕВОЙ пак.
- photon_v1.3b_IRLights — где шло MSM-dev (glsl теперь 1290 после слияния), но настройки STALE (до-ребренд screen.irlite_*).
Слияние Фаза1 (сессия, задачи #1-#3 done): VL_NOISE влит в glsl (база=эталон MSM; 5 деф+2 функции irlite_noise3D/irlite_vlNoise+3 хука march; VL_SHADOW_STRIDE сохранён; препроц 57/57, скобки 75/75), затем мердж-glsl СКОПИРОВАН в активный Photon_IRLights (бэкап старого 891-стр glsl в scratchpad Photon_IRLights_irlite_lights.glsl.bak-premerge). Итог: Photon_IRLights = полный пак (MSM+VL_NOISE glsl + ребренд-настройки), NORMAL_OFFSET внутренний (убран из его screen/sliders/lang).
ГОЧА тиража (важно): ребренд 3b3d79a вывел IRLITE_SHADOW_NORMAL_OFFSET СЛАЙДЕРОМ во ВСЕХ 7 паках. Юзер решил внутренним -> при регене photon.irlights (T5) И тираже на 6 паков (T7) убирать NORMAL_OFFSET из настроек (screen+sliders+lang) + снимать glsl-аннотацию. Иначе БИТЫЙ пустой слайдер (аннотация снята пунктом А3, но опция листнута в настройках) — ровно этот баг всплыл у Photon_IRLights и был починен. Правило: slider-аннотацию `// [...]` в glsl снимать ТОЛЬКО если дефайн НЕ листнут в реальных (ребренд) настройках, а не в stale-эталоне.
Зеркальный случай (2026-07-03): Photon отставал от ребренда и по IRLITE_VL_SHADOW_STRIDE — CR/BSL/их патчи ВЫВОДЯТ его слайдером «Shadow Tap Stride» (screen.IRLIGHTS_VOLUMETRIC сразу после IRLITE_VL_SHADOWS + sliders + lang `option.IRLITE_VL_SHADOW_STRIDE=§dShadow Tap Stride`), а Photon нет (фича пришла 07-03, после ребренда 07-02; референс = patches/complementaryreimagined.irlights, bsl.irlights). ВЫВЕДЕН в Photon_IRLights по референсу (пункт А3 зря снял его аннотацию — восстановлена). NB default: Photon=2 (перф), CR/BSL=1; оставлен 2. При регене photon.irlights и тираже — добавить в настройки Photon-патча.

## ДРЕЙФ (исторический контекст, найдено 2026-07-03 при пункте А)
Source-of-truth дев-цикла ([[sync-workflow]]) = Shadres/Modification/<pack> -> copy в run/shaderpacks. Для Photon это НАРУШЕНО: run/shaderpacks/photon_v1.3b_IRLights (1249 стр после А) = текущий эталон с MSM point-стеком (рефактор 2026-07-03), а Shadres/Modification/Photon (891 стр) СТАРЫЙ — БЕЗ MSM point-стека, зато С IRLITE_VL_NOISE + IRLITE_OUTLINE вкл + VL_STEPS 48 + irlite_rotationPhi() без аргумента. Ветки разошлись по разным осям, ни один файл не супермножество другого. Точечный рефактор шёл прямо в run/ (hot-reload), в Shadres/Mod не портирован. ХАЗАРД: photon-патч регенерится вручную/диффом Modification vs Original (gen-photon-patch.ps1 нет) -> регенерация СЕЙЧАС потеряет весь MSM point-стек. Перед тиражом/патч-регеном Photon сперва синкнуть run/ эталон -> Shadres/Modification/Photon и решить судьбу VL_NOISE (есть только в Shadres/Mod). В рамках пункта А НЕ трогалось (правился только run/ эталон, он git-tracked).

## Б — far-floor трёхсторонний рассинхрон (единств. корректностный, degenerate-триггер)
Severity minor. Триггер: point-лампа radius<0.15 (ничтожный свет -> визуально не проявляется на реальных входах). Кросс-каттинг (спот та же болезнь). Один и тот же stored depth линеаризуется против РАЗНЫХ far:
- бейк-проекция: ShadowRenderer.java:173 `far=Math.max(radius, NEAR+0.1)` = 0.15 (NEAR=0.05, ShadowRenderer.java:41). Спот-аналог :131 `Math.max(range, NEAR+0.1)`.
- MSM-декод: PointShadowEvsm.java:193 `slotFar[slot]=Math.max(radius, 0.1f)` (уходит в CONVERT uniform far, :243).
- GLSL-сравнение: irlite_lights.glsl:534 `float radius=max(light.posRadius.w, 0.001)` (тж :318 спот, :1109 VL).
Fix: выбрать ЕДИНЫЙ floor и применить во всех трёх (напр. поднять GLSL/декод до бейк-floor `max(radius, NEAR+0.1)=0.15`, или наоборот — но бейк-floor NEAR+0.1 обязателен чтобы far>near для перспективы, так что канон = 0.15 везде). ЛИБО осознанно задокументировать «radius<0.15 не поддерживается» и заклампить radius на входе. Внимание: floor меняет [near,far] окно нормализации -> перепроверить что не сдвигает тени валидных ламп (radius>=0.15 не затронуты, floor неактивен).

## В — overlay-перф point (Java, общий; = известный open Ф2a)
Severity minor (перф). Не блокер тиража, в GLSL-тираж не входит (irl-core общий -> один фикс на всех паках).
- ShadowBaker.java:723-724: overlay-ветка point каждый кадр зовёт `PointShadowPyramid.markDirty(myLayer)` + `PointShadowEvsm.markDirty(myLayer, radius)` безусловно, затем :737-738 `flushDirty()` безусловно. markDirty берёт ТОЛЬКО slot (нет face-mask) -> flushDirty ребилдит все 6 граней на каждом мипе каждый overlay-кадр (MEDIUM FACE_SIZE=512: ~160 dispatch-faces/slot/кадр MSM + ~54 пирамида), даже если движущийся актёр тронул 1-2 грани. staticBakeBudget (T2.4) гейтит только allowStaticBake (depth-рендер), не префильтр-flush -> пересчёт неограничен по slot и face.
- Fix-направления (из плана Ф2a ОТКРЫТЫЕ ХВОСТЫ): (a) прокинуть face-mask в markDirty(slot, faceMask) и пересчитывать только тронутые грани (dynFaceMaskScratch/lastFaceDynamic уже считается для depth-копий T1.2, но отбрасывается перед префильтром); (b) CPU max-penumbra cull по bulbSize; (c) учесть в shadowBakeBudget, не усугубить спайк C10 ~320ms. Часть пересчёта легитимна (live-куб меняется каждый overlay-кадр) — цель — не гнать 4-5 нетронутых граней.

## Г — стейл-доки/комменты (info/nit, docs-only, 0 рантайм-эффекта)
Severity info/nit. Правят рассинхрон «сказано 2D_ARRAY, реально CUBE_MAP_ARRAY view» (раунд-4 сменил биндинг, комменты отстали). Лочстеп: править в ОБОИХ модах.
- Г1 PointShadowEvsm.java:19 джавадок класса «rebound to 2D_ARRAY by the host mixins» -> CUBE_MAP_ARRAY view (см. getGlTextureId() джавадок :179-181 = уже верный, противоречие внутри файла).
- Г2 ProgramSamplersBuilderMixin.java:39 (аддон) `// F2b: ... rebound to GL_TEXTURE_2D_ARRAY` -> CUBE_MAP_ARRAY. Идентичный стейл в редакторе: irlights/.../ProgramSamplersBuilderMixin.java:42. Реальный таргет = SamplerBindingCubeArrayMixin:75 `bindTextureToUnit(GL_TEXTURE_CUBE_MAP_ARRAY,...)`.

## Д — спот-канал парити (кандидат в тот же заход что тираж/перевод спота на MSM)
Не находка ревью, а перенос: у спота те же корни, что чинились у точки, но спот остался на EVSM+Чебышёв.
- far-floor: спот ShadowRenderer.java:131 та же `NEAR+0.1` vs GLSL:318 `0.001` — тот же рассинхрон Б.
- у спота НЕТ slope-aware tapMargin в blocker search и НЕТ contact-weighting (у точки есть, irlite_lights.glsl ~664-698) -> те же grazing-ложные-блокеры.
- E4-биас у спота консистентен (irlite_depthBias с zPersp), это ок.
- Решение отложено до тиража (план ТИРАЖ: «спот-канал на MSM/root-фиксы = кандидат тиража»). Делать только по прямой команде.

## НЕ ТРОГАТЬ — проверено верным adversarial-верификацией (не «чинить»!)
Эти места рейзились как подозрительные и были ОПРОВЕРГНУТЫ (>=2 верификатора). Не регрессировать «фиксами»:
- irlite_lights.glsl:181 `mix(b, vec4(0.5), 1e-4)` MSM moment-bias таргет = КОРРЕКТЕН (держит Ганкель-матрицу PD, ~2.5e-5 floor, доминирует fp32-cancellation). vec4(0.5) не ошибка.
- irlite_lights.glsl:195-204 Hamburger quadratic: NaN при c.z=0 (Dirac lz=0/zf=0) НЕВОЗМОЖЕН — moment-bias регуляризует моменты, точного коллапса нет. Гарды max(D22,1e-12)/max(D33D22,1e-24) достаточны.
- PointShadowEvsm.java:87 NDC-ремап `ndcZ=z01*2-1` ЕСТЬ и верен; линеаризация консистентна с GLSL.
- E4-биас = zPersp сквозь surface (irlite_lights.glsl:568) + VL (:1083) — верно, не euclid.
- Пирамидный plane-margin (irlite_lights.glsl:650-651) leak-safe: `max(dot(nN,Loff),0.2)` фейлит в over-dark, не в утечку.
- validity-гейт mm.z<0 на обеих surface-ветках (soft :730 + fast/hard :590); VL корректно без гейта (raw-depth irl_pointShadowArray, irl_pointEvsm не читает).
- mixin'ы addon<->redactor семантически идентичны (единственный «дрейф» = одинаковый стейл-коммент Г2, правится вместе).
- IRLITE_MSM_MOMENT_BIAS = 1.0e-4 дефолт корректен (память «3e-7» была стейл).
