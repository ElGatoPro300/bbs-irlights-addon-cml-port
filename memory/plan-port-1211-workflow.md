---
name: plan-port-1211-workflow
description: Workflow порта irl-core (новая ветка 1.21.1) и bbs-irlights-addon (merge master -> port/1.21.1) на MC 1.21.1 ВЫПОЛНЕН 2026-07-06 (оба build зелёные, 0 blocker/major); результаты НЕЗАКОММИЧЕНЫ в worktree _wt-irl-core-1.21.1 и _wt-addon-1.21.1, коммиты после подтверждения юзера. Промпты воркеров сохранены ниже.
metadata: 
  node_type: memory
  type: project
  originSessionId: e5df9e19-f445-4923-9173-18ed90e1254e
---

Статус 2026-07-06 ФИНАЛ: WORKFLOW ВЫПОЛНЕН (wf_b20afbc9-a3f, 7 агентов R1/R2/C1/A1/V1-V3, фикс-раунд не понадобился: 0 blocker/major), оба build зелёные, гейт (build+JiJ) пройден, ЗАКОММИЧЕНО по подтверждению юзера после ребута ПК: core ветка 1.21.1@80a3986 (от main cf4ad94), addon port/1.21.1@688afda (merge master 3c3ef3f, предок cc8b856). Push НЕ делался. Worktree _wt-* оставлены (аддонный нужен для ин-гейм runClient-проверки 1.21.1; убрать git worktree remove после неё). NB после ребута git-identity оказался пуст (глобальный конфиг потерян) — восстановлен ЛОКАЛЬНО в обоих репо: user.name=qualet, user.email=qualetprod@gmail.com (как в прошлых коммитах); редактор irlights не трогал — при коммитах там проверить. ОТКРЫТО: ин-гейм проверка 1.21.1 (за юзером), решение по worktree remove, follow-ups ниже. Детали состояния (на момент коммита):
- irl-core: worktree C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-irl-core-1.21.1, НОВАЯ ветка 1.21.1 от main cf4ad94, незакоммичено РОВНО 2 файла: build.gradle (Loom 1.15.5, Java 21, MC 1.21.1, yarn 1.21.1+build.3:v2, Iris 1.8.8+1.21.1-fabric, loader 0.16.14 сохранён, gradle.properties НЕ тронут) + light/shadow/ShadowRenderer.java (buffer-rework: Tessellator.getInstance().begin() одношагово x2, BuiltBuffer top-level импорт, 24x .next() удалены, getModelViewStack()=Matrix4fStack: identity/mul/pushMatrix/popMatrix; ванильный MatrixStack поля scratch сохранён). Остальные 36/37 java без правок (patcher MC-нейтрален, GL-only фильтры Evsm/Pyramid/Atlas/CookieArrayBase чисты, Iris 2-арг addDynamicSampler жив на 1.8.8, EntityRenderDispatcher.configure 3-арг актуален). build+publishToMavenLocal SUCCESSFUL; intermediary подтверждён javap (net/minecraft/class_*, named-утечек нет).
- Аддон: worktree C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-addon-1.21.1 на port/1.21.1, merge --no-commit master ВЫПОЛНЕН (MERGE_HEAD=3c3ef3f сохранён, конфликты разрешены, git add сделан). git diff master = РОВНО 11 файлов (build.gradle, gradle.properties, fabric.mod.json + 8 java API-дельт: IRLiteBbsCasterSource rotate2, LightGuideRenderer, SpotlightFormRenderer, SpotGuideDrag, IRLightPositionResolver, UIPointLightFormPanel, UISpotlightFormPanel, GameRendererLightMixin на RenderTickCounter.getTickDelta(true)). patches/*.irlights байт-идентичны master (hash-object сверен V1). build/libs/irlite-1.1.jar с JiJ META-INF/jars/irl-core-1.1.jar (114085 B) — переверифицирован оркестратором (V1 clean-компиляцией стёр сборку A1, пересобрано BUILD SUCCESSFUL).
- НОВЫЕ 1.21.1-факты (их НЕ БЫЛО в каталоге дельт; master писался под BBS 2.3.1, на 1.21.1 только BBS 2.2.1): BBS 2.2.1-1.21.1 НЕ имеет UISection (панели форм уплощены до this.options.add, ВСЕ контролы включая cookie/gobo-набор сохранены) и НЕ имеет UIReplaysEditor.isActionsMode() (гард снят в SpotGuideDrag — ReplayCategory на 1.21.1 = PLAYER/MODEL/POSE, ACTIONS-таймлайна нет). Плюс 2 мёрж-артефакта авто-слияния исправлены A1: дублированный cookie-блок LightCollector + второй buffer-emit сайт LightGuideRenderer без 1.21-рерайта.
- Верификация: V1 PASS (потерь master-контента нет: film-guides 3913622, cookie LRU, bone-fix 2004219, Tier1+Tier2 3c3ef3f, switch(outcome) — всё на месте), V2 PASS (порт-дельты не потеряны; NB structured-вывод V2 дегенеративен findings=[test], но verdict дан после 33 tool-calls реальной проверки), V3 APPROVE (core-diff чист; added/removed строки ShadowRenderer байт-идентичны editor-референсу; 1.20.4-остатки только в неизменённых README/docs/старых комментах).
- mavenLocal org/qualet/irl-core/1.1 теперь = 1.21.1-intermediary (перезаписал 1.20.4) — перед сборкой 1.20.x-линий re-publish core main (per-MC publish-before-build).
СБОРКА ТРИЛОГИИ 1.21.1 ВЫПОЛНЕНА 2026-07-06 (по команде юзера): core+addon из worktree (уже были зелёные) + редактор irlights port/1.21.1@f85113e собран в НОВОМ worktree _wt-editor-1.21.1 БЕЗ правок — BUILD SUCCESSFUL против core main cf4ad94 через composite includeBuild("../irl-core") (Tier1+Tier2 оказались аддитивны для поверхности порт-ветки; E3-перевод редактора на per-version core по-прежнему открыт). Рантайм-нюанс: в jar редактора JiJ-вшит irl-core-1.1 сборки MAIN (линия 1.20.4) — приемлемо: fabric.mod.json ядра БЕЗ MC-констрейнта (environment *, depends нет), а порт-ветка редактора зовёт только MC-нейтральную поверхность ядра (LightBuffer/Registry/patcher; своё shadow-дерево C20), MC-типизированные классы ядра не загружаются. Доставка: C:/Users/Qualet/Desktop/IRLights/1.21.1/ = irl-core-1.1-mc1.21.1.jar + irlite-addon-1.21.1.jar (JiJ core-1.21.1) + irl-redactor-1.21.1.jar (JiJ core-main). Папка Desktop/IRLights была пуста/удалена — создана заново, только 1.21.1.
Шаги (1) commit core и (2) commit merge аддона ВЫПОЛНЕНЫ 2026-07-06 (80a3986 / 688afda). (3) Ин-гейм проверка 1.21.1 подтверждена юзером («всё норм») + все 3 worktree УДАЛЕНЫ 2026-07-06. Готча удаления worktree на Windows: git worktree remove --force падает («Filename too long»/«Invalid argument») на глубоких путях .gradle/loom-cache, а rm -rf ловит «Device or resource busy» от живых Gradle-демонов — порядок: gradlew --stop (обе линии демонов, 9.2.0 и 9.2.1), затем rm -rf, затем git worktree prune во всех репо. ПОРТ 1.21.1 ЗАКРЫТ; открыты только follow-ups: build-trilogy.ps1 (нет publish-шага per-MC core, шапка устарела), fabric.mod.json license всё ещё CC-BY-ND-4.0 (MIT только в LICENSE/README — вне скоупа порта), косметика неиспользуемых импортов SpotlightFormRenderer/IRLightPositionResolver, Loom-warning semver «1.1», ин-гейм runClient-проверка 1.21.1 за юзером.

Статус 2026-07-05 (истории ради): оркестрация подготовлена, запуск был прерван юзером до вызова Workflow. Прямая команда юзера на порт есть (гейт "порт только по прямой команде" пройден). Редактор irlights В СКОУП НЕ ВХОДИТ.

## Задача
Портировать irl-core и bbs-irlights-addon на MC 1.21.1. Для irl-core — НОВАЯ ветка с именем ровно "1.21.1" (не port/1.21.1). Для аддона — обновить существующую ветку port/1.21.1 до текущего master + перевести на per-version core.

## Грунт-факты разведки (проверены по git 2026-07-05, повторно не собирать)
- irl-core: единственная ветка main, HEAD cf4ad94 (Tier1+Tier2), tag v1.1, статус чист. build.gradle: Loom 1.9-SNAPSHOT, Java 17 (source/target/release), minecraft 1.20.4, yarn 1.20.4+build.1:v2, fabric-loader 0.16.14, modCompileOnly maven.modrinth:iris:1.7.2+1.20.4, version из gradle.properties irl_core_version=1.1, publishToMavenLocal публикует remapped intermediary jar. Gradle wrapper 9.2.0. 38 java-файлов в src/main/java/org/qualet/irl: light/{CookieArrayBase,FramePipeline,IrlSamplers,LightBuffer,LightMath,LightRegistry}, light/iris/{IrisShadersState,IrlSamplersBind}, light/shadow/{BlockShadowCache,BlockShadowCollector,BlockShadowEntry,CasterType,IRLShadowQuality,ImmediateOccluderBatch,OccluderBatch,OccluderSink,PointShadowArray,PointShadowEvsm,PointShadowPyramid,ShadowBakeState,ShadowBaker,ShadowCasterSource,ShadowConfig,ShadowEngine,ShadowRenderer,SpotShadowEvsm,SpotShadowPyramid,SpotlightDepthAtlas}, patcher/{IrlPatch,IrlPatchApplier,IrlPatchParser,PatchEngine,PatchLibrary,PatchResult,Patcher,PatcherHost,Shaderpacks}.
- Аддон bbs-irlights-addon: master checked out (НЕ трогать: у юзера локальное D run/shaderpacks/photon_v1.3b_IRLights/... — не коммитить). merge-base master..port/1.21.1 = 4e98833. master впереди на 56 коммитов (Tier1+Tier2 adoption 3c3ef3f, тонкие миксины, Outcome-UI, cookie LRU, полный стек shadow-фильтрации во всех patches/*.irlights, film-guides 3913622, bone-fix 2004219, BBS 2.3.1 bump c6251d6). port/1.21.1 несёт 12 своих коммитов: 9109e38 порт на 1.21.1, c91c4c4 host-cell skip, 03a04f6+1b5335c gobo/cookie (конвергентно master), dd8a46a 1.0-obt+пин core, 51bd68c MIT+README, 8c8df2b delist IterationRP, e212a9b untrack iterationrp, f4064bc drop java.home, 9085261 hard-depend irl-core, 1b74190 patcher-тексты, cc8b856 icon. Порт-ветка ВСЁ ЕЩЁ на plain-java core: settings.gradle includeBuild("../irl-core") + clientImplementation org.qualet:irl-core:1.0-obt; master = mavenLocal() + modClientImplementation/include org.qualet:irl-core:1.1, includeBuild снят. Gradle wrapper 9.2.1. libs/*.jar в .gitignore (.gitignore:26) — в worktree их НЕ будет, копировать руками. В libs есть bbs-2.2.1-1.21.1.jar (для 1.21.1 только 2.2.1; 2.3.1 существует лишь для 1.20.1/1.20.4).
- Порт-дельта аддона (diffstat 4e98833..port/1.21.1, 28 файлов): java-зоны = LightGuideRenderer(8), SpotlightFormRenderer(4), IRLightPositionResolver(8), LightCollector(11), CookieArray(+235 новый), BlockShadowCache(8), BlockShadowCollector(12), IRLiteBbsCasterSource(7), ShadowRenderer(86), UISpotlightFormPanel(22), UIPatcherSection(87), GameRendererLightMixin(10), ProgramSamplersBuilderMixin(4), SamplerBindingCubeArrayMixin(18), SpotlightForm(18) + build.gradle(29), gradle.properties(9), settings.gradle, fabric.mod.json(3), README(36), LICENSE(нов)+LICENSE.txt(удал), icon.png, .gitignore(5), patches/*.irlights(по ~59 — устаревшие, master побеждает).
- Редактор irlights (READ-ONLY референс): ветки main, port/1.20.1, port/1.21.1, port/1.21.4, port/1.21.11. port/1.21.1: Loom 1.15-SNAPSHOT, Gradle 9.2.0, gradle.properties: minecraft 1.21.1, yarn 1.21.1+build.3, loader 0.16.14, fabric 0.116.12+1.21.1. Несёт СВОЁ 1.21.1-адаптированное shadow-дерево src/client/java/org/qualet/irlredactor/light/shadow/ (самодостаточная ветка, C20). На main эти файлы удалены Ф2-коммитом d8705f4 (родитель d8705f4^ = последняя ревизия с ними) — diff родителя против port/1.21.1 по этому дереву = чистейший каталог 1.21.1-дельт оркестрации теней. GameRendererLightMixin порт-ветки = референс сигнатуры inject 1.21.1.
- Координаты 1.21.1 (канон из обеих порт-веток): Loom 1.15.5 (оба потребителя на 1.15 -> правило наистаршего Loom даёт 1.15; связка Loom 1.15.5 + Gradle 9.2.x подтверждена аддоном), Java 21, minecraft 1.21.1, yarn 1.21.1+build.3:v2, fabric-loader 0.16.14 (core) / loader_version=0.19.3 (аддон property), fabricApi 0.116.12+1.21.1, iris maven.modrinth:iris:1.8.8+1.21.1-fabric (1.8.8 держит 2-арг addDynamicSampler; 4-арг только в 1.9+/1.10.7), sodium mc1.21.1-0.6.13-fabric, bbsJar libs/bbs-2.2.1-1.21.1.jar, lwjglPin null.
- Известные API-дельты 1.20.4->1.21.1 (из памяти project-port-1211): buffer-rework 1.21.0 (Tessellator.begin -> Tessellator.getInstance().begin(mode,format) возвращает BufferBuilder; .next() удалён; draw -> BufferRenderer.drawWithGlobalProgram(builder.end())); Transform.rotate2 ПРИСУТСТВУЕТ в bbs-2.2.1-1.21.1 -> восстановить в IRLiteBbsCasterSource.modelBlockHash (+ сайты LightCollector/IRLightPositionResolver); GameRenderer.renderWorld сигнатура сменилась (RenderTickCounter, tickDelta из counter'а — точную форму взять из редакторского миксина); MatrixStack и RenderSystem на 1.21.1 ЖИВЫ (умирают в 1.21.5+); RenderLayers.getBlockLayer без изменений; EntityRenderDispatcher.configure ещё 3-арг (World,Camera,Entity); 1.21.1 имеет реал-модельные тени энтити (pre-1.21.5 путь) — near-lockstep с main.
- build-trilogy.ps1 УСТАРЕЛ (шапка ещё про composite includeBuild; publish-шага для core нет) — follow-up после порта.

## Решения оркестратора (зафиксированы)
1. Работа в git worktree, основные деревья НЕ трогаются: CORE_WT = BBS/_wt-irl-core-1.21.1 (git -C irl-core worktree add -b 1.21.1 <dir> main), ADDON_WT = BBS/_wt-addon-1.21.1 (worktree add <dir> port/1.21.1). Оба пути свободны (проверено).
2. БЕЗ КОММИТОВ (feedback commit-checkpoints): merge через --no-commit, разрешение + git add, MERGE_HEAD остаётся; коммит только после подтверждения юзера. Ветка core 1.21.1 создаётся worktree'ем (-b), правки некоммичены.
3. Координата ядра ОСТАЁТСЯ org.qualet:irl-core:1.1 на ветке 1.21.1 (конвенция per-MC publish-before-build; mavenLocal перезаписывается — перед сборкой другой MC-линии нужен re-publish соответствующей ветки core). Альтернатива (версия с MC-суффиксом) отвергнута ради симметрии с 1.20.4.
4. Аддон: master побеждает структурно (Tier1/Tier2-shape, patches/ байт-в-байт), порт-ветка побеждает в брендинге (MIT LICENSE, README, icon, hard-depend, тексты патчера при отсутствии эквивалента в master), 1.21.1-API-дельты накатываются ПОВЕРХ master-версий файлов. mod_version итог = 1.1 (master bump).
5. Файлы, удалённые master'ом (Ф2-вынос: qualet/irlite/client/light/shadow/*, старый CookieArray-монолит, IrisShadersState) — принять удаление; их 1.21.1-дельты уезжают в core-ветку 1.21.1.
6. Роутинг по сложности: R1/R2 = sonnet (MEDIUM), C1/A1/V1-V3/F = opus (HIGH, effort high). haiku-задач нет (механика поглощена R2).
7. Зависимости: R1 || R2 -> C1 -> A1 (аддону нужен опубликованный core-1.21.1 в mavenLocal) -> V1 || V2 || V3 -> F (только при blocker/major) -> re-verify.
8. Гейт сессии = build зелёный обоих + JiJ-проверка; runClient/ин-гейм — за юзером.

## Структура Workflow (скелет скрипта)
meta.phases: Разведка / Порт irl-core / Порт аддона / Верификация / Фиксы. В скрипте пути задавать ПРЯМЫМИ слэшами (C:/...) — обратные слэши в JS template literals дают SyntaxError на \U. Плейсхолдер {SCRATCH} заменить на scratchpad-директорию ТЕКУЩЕЙ сессии запуска (session-specific!). Константы: CORE=C:/Users/Qualet/Documents/Project/Minecraft/BBS/irl-core, ADDON=.../bbs-irlights-addon, EDITOR=.../irlights, CORE_WT=.../BBS/_wt-irl-core-1.21.1, ADDON_WT=.../BBS/_wt-addon-1.21.1.
Поток: const [surface, deltas] = await parallel([R1,R2]) -> const core = await agent(C1+JSON(surface,deltas), opus/high) -> if blocked return -> const addon = await agent(A1+JSON(deltas)+core.summary, opus/high) -> if blocked return -> const reviews = await parallel([V1,V2,V3]) -> blockers = findings blocker|major -> if any: F(opus) + один re-verify -> return всё.

JSON-схемы:
- SURFACE: {summary:string, files:[{path,needsPort:boolean,apis:[string],notes}]}
- DELTAS: {catalog:[{area,change,files:[string]}], dumps:[string], notes:string}
- REPORT: {status:'ok'|'blocked', summary, buildResult, changedFiles:[string], conflicts:[string], blockers:[string], followUps:[string]}
- FINDINGS: {verdict:string, findings:[{severity:'blocker'|'major'|'minor'|'nit', file, summary, detail}]}

ОБЩИЙ БЛОК ПРАВИЛ (вставлять в КАЖДЫЙ промпт):
"ЖЕЛЕЗНЫЕ ПРАВИЛА: (1) НИКОГДА не выполняй git commit / git push / git tag — вся работа остаётся незакоммиченной. (2) НЕ трогай основные рабочие деревья: C:/Users/Qualet/Documents/Project/Minecraft/BBS/bbs-irlights-addon стоит на master с локальными изменениями юзера; .../irl-core остаётся на main; .../irlights только для чтения. (3) Пиши только в свой worktree и в {SCRATCH}. (4) Gradle: запускай gradlew.bat из каталога worktree, timeout 600000 мс, полный лог сохраняй в {SCRATCH}. (5) Пути даны прямыми слэшами — это валидные Windows-пути."

## ПРОМПТ R1 (sonnet, label recon:core-surface, schema SURFACE)
Read-only разведка, НИЧЕГО не менять. Репозиторий irl-core: C:/Users/Qualet/Documents/Project/Minecraft/BBS/irl-core (ветка main, HEAD cf4ad94). Это Fabric/Loom-модуль под MC 1.20.4 (yarn), Java 17: свет SSBO + .irlights-патчер + оркестрация теней. Задача: инвентаризация MC-поверхности ВСЕХ 38 .java файлов в src/main/java/org/qualet/irl/** для порта на MC 1.21.1 (yarn 1.21.1+build.3, Iris 1.8.8). Для КАЖДОГО файла: (а) импорты/использования net.minecraft.*, com.mojang.*, net.irisshaders.*, net.fabricmc.* (org.lwjgl/org.joml/java.* MC-нейтральны, но пометь GL-heavy файлы); (б) needsPort=true если использует API, менявшиеся 1.20.4->1.21.1. Горячие зоны 1.21.0 buffer-rework: Tessellator.begin(...) -> Tessellator.getInstance().begin(mode,format) возвращает BufferBuilder; BufferBuilder .next() УДАЛЁН; Tessellator.draw()/BufferBuilder.end() -> BufferRenderer.drawWithGlobalProgram(builder.end()). Также проверь использование Camera / EntityRenderDispatcher (configure на 1.21.1 ещё 3-арг) / ClientWorld / RenderLayers (getBlockLayer без изменений) / MatrixStack и RenderSystem (на 1.21.1 живы, но отдельные методы могли сдвинуться) / VertexBuffer / VertexFormat(s). (в) notes: конкретные методы/строки под замену. Особое внимание: light/shadow/ShadowRenderer.java (тяжёлый GL+MC), shadow/ShadowBaker.java, shadow/BlockShadowCollector.java, light/FramePipeline.java, light/iris/IrisShadersState.java + IrlSamplersBind.java (на Iris 1.8.8 ProgramSamplers.Builder.addDynamicSampler остаётся 2-аргументным как в 1.7.2 — проверь что код не зовёт ничего сверх), light/CookieArrayBase.java, новые shadow/{PointShadowEvsm,PointShadowPyramid,SpotShadowEvsm,SpotShadowPyramid}.java (их ни одна порт-ветка не видела). В summary — оценка объёма порта. + ОБЩИЙ БЛОК ПРАВИЛ.

## ПРОМПТ R2 (sonnet, label recon:1211-deltas, schema DELTAS)
Read-only разведка по git-истории; файлы репозиториев НЕ менять; дампы писать в {SCRATCH}. Контекст: портируем irl-core (main, MC 1.20.4) и bbs-irlights-addon (master) на MC 1.21.1; в репах есть СТАРЫЕ порт-ветки с готовыми 1.21.1-адаптациями — извлеки из них точный каталог API-дельт 1.20.4->1.21.1.
Источники:
1. АДДОН C:/Users/Qualet/Documents/Project/Minecraft/BBS/bbs-irlights-addon, merge-base master..port/1.21.1 = 4e98833. Сделай git -C <addon> diff 4e98833 port/1.21.1 -- "src" > {SCRATCH}/addon-port-delta.diff. Классифицируй: (a) buffer-rework Tessellator/BufferBuilder/draw; (b) BBS Transform.rotate2-восстановление (IRLiteBbsCasterSource.modelBlockHash, LightCollector, IRLightPositionResolver); (c) GameRendererLightMixin — точная сигнатура inject-таргета GameRenderer.renderWorld на 1.21.1 и способ получения tickDelta; (d) iris-миксины ProgramSamplersBuilderMixin/SamplerBindingCubeArrayMixin; (e) cookie/gobo — НЕ версионная дельта (конвергентная фича, master уже несёт) — отметь и пропусти; (f) брендинг README/LICENSE/icon/fabric.mod.json — перечисли.
2. РЕДАКТОР C:/Users/Qualet/Documents/Project/Minecraft/BBS/irlights (READ-ONLY): ветка port/1.21.1 несёт 1.21.1-адаптированное дерево src/client/java/org/qualet/irlredactor/light/shadow/*.java. На main эти файлы удалены коммитом d8705f4; возьми родителя (git rev-parse d8705f4^) и сделай попарный diff d8705f4^ vs port/1.21.1 по этому дереву > {SCRATCH}/editor-shadow-1211-delta.diff. Это чистейший каталог 1.21.1-дельт оркестрации теней (канон = редактор).
3. Полные референсы (git show ветка:путь > файл): editor port/1.21.1 GameRendererLightMixin.java -> {SCRATCH}/ref-gamerenderer-mixin-1211.java; editor port/1.21.1 light/shadow/ShadowRenderer.java -> {SCRATCH}/ref-editor-shadowrenderer-1211.java; addon port/1.21.1 src/client/java/qualet/irlite/client/light/shadow/ShadowRenderer.java -> {SCRATCH}/ref-addon-shadowrenderer-1211.java; addon port/1.21.1 IRLiteBbsCasterSource.java -> {SCRATCH}/ref-addon-castersource-1211.java; addon port/1.21.1 LightGuideRenderer/SpotlightFormRenderer/LightCollector/IRLightPositionResolver -> {SCRATCH}/ref-addon-<имя>-1211.java.
4. Верни catalog (для каждого паттерна: было 1.20.4 -> стало 1.21.1, затронутые файлы, источник-дамп), dumps (пути), notes (ОБЯЗАТЕЛЬНО: полный текст сигнатуры inject-метода GameRendererLightMixin 1.21.1 + вывод tickDelta). + ОБЩИЙ БЛОК ПРАВИЛ.

## ПРОМПТ C1 (opus, effort high, label port:irl-core, schema REPORT; в промпт вшить JSON R1+R2)
Ты портируешь общее ядро irl-core на Minecraft 1.21.1 в НОВОЙ ветке "1.21.1". CORE=C:/Users/Qualet/Documents/Project/Minecraft/BBS/irl-core (main @ cf4ad94, tag v1.1): Fabric/Loom-модуль — свет SSBO (org.qualet.irl.light), Iris-биндинги (light.iris), оркестрация теней (light.shadow, включая свежий MSM4/EVSM/pyramid-стек), .irlights-патчер (org.qualet.irl.patcher, чистая Java).
Шаг 0: git -C <CORE> worktree add -b 1.21.1 "C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-irl-core-1.21.1" main. Дальше вся работа ТОЛЬКО в worktree.
Шаг 1 — сборочная конфигурация (gradle.properties НЕ трогать — версия остаётся 1.1): fabric-loom '1.9-SNAPSHOT' -> '1.15.5' (оба потребителя 1.21.1 на Loom 1.15; связка Loom 1.15.5 + Gradle 9.2.x подтверждена аддоном); Java 17 -> 21 везде (sourceCompatibility/targetCompatibility/options.release) + поправь комментарий про lowest-consumer (на этой ветке потребители Java 21); minecraft 1.20.4 -> 1.21.1; yarn 1.20.4+build.1 -> 1.21.1+build.3 (:v2 сохранить); fabric-loader 0.16.14 оставить; modCompileOnly maven.modrinth:iris:1.7.2+1.20.4 -> maven.modrinth:iris:1.8.8+1.21.1-fabric (1.8.8 держит 2-арг addDynamicSampler); обнови шапку-комментарий build.gradle (эта ветка = линия MC 1.21.1).
Шаг 2 — порт Java. Сохранить ВСЁ поведение main (MSM4 Hamburger, seamless cube-view, EVSM/pyramid, adaptive/gather) — менять только API-вызовы. Вшитые результаты разведки: SURFACE=<JSON R1>, DELTAS=<JSON R2>; дампы в {SCRATCH} (editor-shadow-1211-delta.diff = канонический список правок для BlockShadowCache/Collector/ShadowBaker/ShadowRenderer/SpotlightDepthAtlas/PointShadowArray и пр.). ВАЖНО: тела файлов в core main НОВЕЕ референсов (MSM4/cube-view/EVSM добавлены позже) — применяй дельты как ПАТТЕРНЫ, не как построчный патч. Новые классы PointShadowEvsm/PointShadowPyramid/SpotShadowEvsm/SpotShadowPyramid проверь сам (вероятно raw GL -> без правок). Паттерны 1.21: buffer-rework (begin/vertex/next/draw); MatrixStack/RenderSystem живы; EntityRenderDispatcher.configure 3-арг; RenderLayers.getBlockLayer без изменений; Iris-биндинги вероятно без правок — подтверди компиляцией.
Шаг 3 — гейт: в worktree .\gradlew.bat build (timeout 600000; лог -> {SCRATCH}/core-build.log); затем .\gradlew.bat publishToMavenLocal; проверь артефакт %USERPROFILE%/.m2/repository/org/qualet/irl-core/1.1/ (перезапись 1.20.4-артефакта ОЖИДАЕМА — конвенция per-MC publish-before-build); желательно javap-проверка что jar в intermediary (net/minecraft/class_*). JDK 21 при нужде: C:/Users/Qualet/.jdks/ms-21.0.11.
Верни REPORT: status, buildResult, changedFiles (каждый с одним словом почему), blockers, followUps. + ОБЩИЙ БЛОК ПРАВИЛ.

## ПРОМПТ A1 (opus, effort high, label port:addon, schema REPORT; в промпт вшить JSON R2 + summary C1)
Ты обновляешь порт-ветку BBS-аддона IRLite (MC 1.21.1) до текущего master и переводишь её на per-version ядро. ADDON=C:/Users/Qualet/Documents/Project/Minecraft/BBS/bbs-irlights-addon. master = 56 коммитов вперёд от merge-base 4e98833 (Tier1+Tier2 adoption, тонкие миксины-делегаты, Outcome-UI патчера, cookie LRU, все актуальные patches/*.irlights с полным стеком shadow-фильтрации, film-guides, bone-fix); port/1.21.1 = 12 коммитов (порт 1.21.1 + брендинг релиза: MIT LICENSE, README, icon, hard-depend irl-core, consumer-friendly тексты патчера, gobo/cookie-предок).
Шаг 0: git -C <ADDON> worktree add "C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-addon-1.21.1" port/1.21.1. libs/*.jar в .gitignore -> скопируй ВСЕ jar из <ADDON>/libs/ в worktree/libs/ (минимум bbs-2.2.1-1.21.1.jar).
Шаг 1: в worktree git merge --no-commit master. Правила разрешения:
1. Код и патчи — master побеждает структурно. patches/*.irlights БАЙТ-В-БАЙТ из master (после merge: git diff master -- patches/ ПУСТ; iterationrp gitignored — его нет в git обеих веток). Java: master-версии (тонкий GameRendererLightMixin-делегат FramePipeline, IrliteShadowConfig, CookieArray extends CookieArrayBase, IrlSamplers-биндинги, UIPatcherSection на switch(outcome)).
2. Поверх master-версий восстанови 1.21.1-дельты (каталог DELTAS=<JSON R2>, референсы в {SCRATCH}): rotate2 — LightCollector, IRLightPositionResolver и modelBlockHash в src/client/java/org/qualet/irl/light/shadow/IRLiteBbsCasterSource.java (master ПЕРЕМЕСТИЛ файл из qualet/irlite/client/light/shadow/ — при modify/delete бери master-путь и вручную перенеси rotate2 из {SCRATCH}/ref-addon-castersource-1211.java); buffer-rework — LightGuideRenderer, SpotlightFormRenderer (референсы ref-addon-*.java), применять к master-версиям НЕ теряя master-фичи (film-editor hover-guides, интерактивные драг-гайды); GameRendererLightMixin — master-версия (тонкий делегат), поменять ТОЛЬКО inject-таргет/сигнатуру на 1.21.1 по {SCRATCH}/ref-gamerenderer-mixin-1211.java (renderWorld + вывод tickDelta); iris-миксины — master как есть (Iris 1.8.8 = 2-арг), чинить только если компиляция упадёт.
3. Файлы, удалённые master'ом (вынос в ядро: qualet/irlite/client/light/shadow/*, старый CookieArray-монолит, IrisShadersState и пр.) — принять удаление (их 1.21.1-дельты уже в core-ветке 1.21.1; отчёт core: <summary C1>).
4. build.gradle: master-структура (mavenLocal(), modClientImplementation + include "org.qualet:irl-core:1.1") + мультиверсионный -Pmc селектор с default '1.21.1': профиль 1.21.1 = minecraft 1.21.1 / yarn 1.21.1+build.3 / fabricApi 0.116.12+1.21.1 / bbsJar libs/bbs-2.2.1-1.21.1.jar / sodium mc1.21.1-0.6.13-fabric / iris 1.8.8+1.21.1-fabric / lwjglPin null; профили 1.20.1/1.20.4 оставить справочно с bbs-2.3.1 (как в master); Java-гейт 21 для 1.21.1 / 17 для 1.20.x (форма из port-версии).
5. settings.gradle: master (includeBuild снят). gradle.properties: minecraft_version=1.21.1, yarn_mappings=1.21.1+build.3, loader_version=0.19.3, mod_version=1.1 (master bump), org.gradle.jvmargs=-Xmx3G.
6. Брендинг — port побеждает: LICENSE (MIT; LICENSE.txt остаётся удалённым), README порта, icon.png порта; consumer-friendly тексты UIPatcherSection объединяй с master-структурой Outcome-switch (если master уже несёт эквивалент — бери master, сравни глазами).
7. fabric.mod.json: объединить port-шаблон (MC 1.21.x диапазон, hard depends irl-core, bbs>=2.2.1 на 1.21.1) + master-добавки.
Шаг 2 — гейт: core-1.21.1 уже в mavenLocal (org.qualet:irl-core:1.1 intermediary 1.21.1). В worktree: .\gradlew.bat build --refresh-dependencies (лог -> {SCRATCH}/addon-build.log, timeout 600000). После сборки проверь в build/libs/irlite-1.1.jar наличие META-INF/jars/irl-core-1.1.jar.
Шаг 3 — самопроверка: git diff master (в worktree) должен показывать ТОЛЬКО 1.21.1-API-дельты, сборочные профили, брендинг. Иной дрейф — исправить или объяснить.
НЕ коммитить (merge оставить: конфликты разрешены, git add сделан, commit НЕТ — MERGE_HEAD сохраняется). Верни REPORT: status, buildResult, conflicts (каждый файл: как решён и почему), changedFiles, blockers, followUps. + ОБЩИЙ БЛОК ПРАВИЛ.

## ПРОМПТ V1 (opus, label verify:master-content, schema FINDINGS)
Adversarial-ревью. В worktree C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-addon-1.21.1 (незакоммиченный merge master -> port/1.21.1 аддона IRLite) ищи ПОТЕРЯННЫЙ master-контент. Основное репо: .../bbs-irlights-addon (master НЕ трогать). Прогони git diff master по worktree: остаток обязан быть ТОЛЬКО 1.21.1-API-дельтами (rotate2, buffer-rework, сигнатура GameRendererLightMixin), сборочными профилями (build.gradle/gradle.properties/settings.gradle/fabric.mod.json) и брендингом (LICENSE/README/icon/тексты патчера). Проверь конкретно: patches/*.irlights байт-идентичны master; тонкий GameRendererLightMixin-делегат FramePipeline сохранён; film-editor hover-guides (коммит 3913622) и интерактивные драг-гайды на месте; cookie LRU; UIPatcherSection switch(outcome); bone-attached fix (2004219); IrliteShadowConfig/IrlSamplers-адаптация Tier1+Tier2 (3c3ef3f) не откатана. Любая потеря master-контента = blocker. Read-only, без правок и коммитов. Верни FINDINGS.

## ПРОМПТ V2 (opus, label verify:port-deltas, schema FINDINGS)
Adversarial-ревью. Проверь, что НИ ОДНА дельта старой порт-ветки не потерялась в worktree C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-addon-1.21.1. Источник: git -C C:/Users/Qualet/Documents/Project/Minecraft/BBS/bbs-irlights-addon diff 4e98833 port/1.21.1 (12 порт-коммитов). Каждая дельта обязана быть либо ПРИСУТСТВУЮЩЕЙ в worktree, либо ОСОЗНАННО замещённой master-эквивалентом (cookie/gobo-функционал конвергентен — master несёт свой; старое shadow-дерево уехало в core-ветку 1.21.1 репо .../irl-core, worktree .../_wt-irl-core-1.21.1 — при проверке rotate2/buffer-rework тени смотри ТУДА). Чек-лист: rotate2 (modelBlockHash IRLiteBbsCasterSource на master-пути org/qualet/irl/light/shadow/ + LightCollector + IRLightPositionResolver), buffer-rework (LightGuideRenderer, SpotlightFormRenderer), 1.21.1-сигнатура GameRendererLightMixin, build.gradle профиль 1.21.1 default + Java-21-гейт, gradle.properties 1.21.1, fabric.mod.json (MC-диапазон + hard depends irl-core), LICENSE MIT + удалённый LICENSE.txt, README порта, icon порта, consumer-friendly тексты патчера, host-cell self-shadow skip (c91c4c4 — если он в block-occluder коде, теперь ищи в core). Потерянная дельта = blocker/major по значимости. Read-only. Верни FINDINGS.

## ПРОМПТ V3 (opus, label verify:core-diff, schema FINDINGS)
Adversarial-ревью порта ядра. Worktree C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-irl-core-1.21.1 (ветка 1.21.1 от main, незакоммичено), основное репо .../irl-core (main, НЕ трогать). git diff main в worktree обязан содержать ТОЛЬКО: build.gradle (Loom 1.15.5, Java 21, minecraft 1.21.1, yarn 1.21.1+build.3:v2, iris 1.8.8+1.21.1-fabric, обновлённые комментарии) + минимальные API-правки в MC-typed файлах. Проверь: (1) поведение shadow-стека НЕ изменено — MSM4 Hamburger / seamless cube-view / EVSM / pyramid / adaptive / gather тела идентичны main с точностью до API-паттернов (buffer-rework и пр.); (2) нет 4-арг addDynamicSampler (Iris 1.8.8 = 2-арг); (3) не осталось ссылок на 1.20.4 (yarn/iris/комменты, где они значимы); (4) patcher/* нетронут (чистая Java); (5) gradle.properties нетронут (irl_core_version=1.1); (6) артефакт %USERPROFILE%/.m2/repository/org/qualet/irl-core/1.1/ опубликован и remapped в intermediary (javap: net/minecraft/class_*); (7) сборочный лог {SCRATCH}/core-build.log = BUILD SUCCESSFUL. Сверь API-паттерны с референсами {SCRATCH}/editor-shadow-1211-delta.diff и ref-*.java. Ошибка порта/потеря поведения = blocker. Read-only. Верни FINDINGS.

## ПРОМПТ F (opus, effort high, label fix-round, schema REPORT; вшить JSON находок blocker+major)
Фикс-раунд порта на MC 1.21.1. Worktrees: ядро C:/Users/Qualet/Documents/Project/Minecraft/BBS/_wt-irl-core-1.21.1 (ветка 1.21.1), аддон .../_wt-addon-1.21.1 (незакоммиченный merge). Находки ревью: <JSON blockers/majors>. Исправь КАЖДУЮ или аргументированно отклони (false positive — с доказательством). После правок ядра: .\gradlew.bat build && publishToMavenLocal в core-worktree, затем пересборка аддона .\gradlew.bat build --refresh-dependencies (логи в {SCRATCH}, timeout 600000). Оба build обязаны быть зелёными. НЕ коммитить. Верни REPORT (fixed/rejected по каждой находке). + ОБЩИЙ БЛОК ПРАВИЛ.

## Инструкция запуска в новой сессии
1. Подставить {SCRATCH} = scratchpad-директория новой сессии (создать при нужде).
2. Проверить прекондишены: worktree-пути _wt-* свободны (git -C <repo> worktree list; при остатках от прежнего запуска — решить с юзером: продолжить в них или git worktree remove); irl-core main = cf4ad94 или новее (если новее — дельту main учесть, промпты не пересобирать, C1 ветвится от актуального main); addon master без новых структурных переездов (иначе освежить правила A1 п.2-3).
3. Собрать Workflow-скрипт по скелету выше (пути прямыми слэшами!), вшить промпты, запустить. После завершения: самому проверить git status обоих worktree + хвосты build-логов, отчитаться юзеру, коммиты ТОЛЬКО после его подтверждения (см. решение 2).
4. После подтверждения юзера: commit в core-worktree (ветка 1.21.1) и commit merge в addon-worktree (port/1.21.1); предложить git worktree remove; follow-ups: build-trilogy.ps1 (publish-шаг per-MC core до сборки каждой линии; шапка устарела), предупредить что mavenLocal irl-core:1.1 теперь intermediary-1.21.1 (перед сборкой 1.20.4-линии re-publish core main).

Связь: [[plan-irl-core-shadow-extraction]] (Ф4 тиражирование — эта задача и есть его 1.21.1-часть), [[project-port-1211]] (дельты 1.21.1), [[reference-edit-routing-by-area]], [[plan-irl-core-library-extraction]] (Tier1+Tier2 состав ядра), [[commit-checkpoints]].
